En este capítulo se describe el desarrollo completo de \textit{The Show Verse}, desde la arquitectura general del sistema hasta la implementación de cada módulo funcional y la integración con las APIs externas. Es la parte más importante del TFG y recoge las decisiones técnicas tomadas durante el proceso de construcción de la plataforma.

% -----------------------------------------------------------------------
\section{Arquitectura del sistema}
% -----------------------------------------------------------------------

\subsection{Estructura de capas}

El sistema se estructura en tres capas diferenciadas, implementadas en una única base de código Next.js:

\textbf{Capa de presentación (Client Components).} Componentes React que se ejecutan y se hidratan en el navegador. Gestionan el estado local de la interfaz, las interacciones del usuario y las peticiones a la capa intermedia. Los componentes de mayor complejidad son los encargados de la gestión del panel de control principal, la vista dinámica de detalles de películas o series y los módulos de contenido del usuario.

\textbf{Capa de servidor (Server Components + API Routes).} Los Server Components de Next.js realizan la búsqueda inicial de datos directamente en el servidor, sin coste de JavaScript para el cliente. Las rutas de la API interna actúan como \textit{proxy} hacia los servicios externos, asegurando que los tokens de autenticación permanezcan privados en el servidor y transformando los datos al formato que requiere la interfaz para su renderizado.

\textbf{Capa de datos (APIs externas).} TMDb, Trakt.tv, OMDb, JustWatch y Plex son las fuentes de los datos principales del catálogo y la información del perfil del usuario.

\subsection{Estructura de carpetas}

La estructura principal se organiza en cuatro directorios bajo \texttt{src/}:

\begin{itemize}
\item \texttt{src/app/} --- Rutas, páginas y API Routes. Cada subdirectorio corresponde a una ruta de la aplicación.
\item \texttt{src/components/} --- Componentes React reutilizables para el diseño de la interfaz y los módulos.
\item \texttt{src/lib/} --- Código reutilizable para conectar con APIs externas y funciones auxiliares comunes.
\item \texttt{src/context/} --- Contextos globales de React para el estado compartido entre componentes.
\end{itemize}

Las rutas principales de la aplicación son:

\begin{table}[!htbp]
\begin{center}
\begin{tabular}{| l | l |}
\hline
\textbf{Ruta} & \textbf{Descripción} \\
\hline
\hline
\texttt{/} & Dashboard principal con carrusel y secciones \\
\hline
\texttt{/details/[type]/[id]} & Página de detalle de película o serie \\
\hline
\texttt{/favorites} & Lista de favoritos del usuario (TMDb) \\
\hline
\texttt{/watchlist} & Lista de pendientes del usuario (TMDb) \\
\hline
\texttt{/history} & Historial de visionado (Trakt) \\
\hline
\texttt{/in-progress} & Series en progreso (Trakt) \\
\hline
\texttt{/stats} & Estadísticas del usuario (Trakt) \\
\hline
\texttt{/calendar} & Calendario de estrenos y próximos episodios \\
\hline
\texttt{/discover} & Búsqueda y filtrado avanzado de contenido \\
\hline
\texttt{/biblioteca} & Librería de contenido (servidor Plex) \\
\hline
\texttt{/lists} & Listas de TMDb, colecciones y listas de Trakt \\
\hline
\texttt{/movies} & Exploración de catálogo de películas \\
\hline
\texttt{/series} & Exploración de catálogo de series \\
\hline
\texttt{/details/person/[id]} & Ficha de actor o director \\
\hline
\end{tabular}
\caption{Rutas principales de la aplicación}
\label{tab:rutas}
\end{center}
\end{table}

% -----------------------------------------------------------------------
\section{Implementación del sistema de autenticación OAuth 2.0}
% -----------------------------------------------------------------------

La autenticación con Trakt se ha implementado con OAuth 2.0 \textit{Authorization Code} y una arquitectura híbrida cliente-servidor, de forma que la parte sensible del proceso queda en el backend y el frontend solo gestiona el estado de conexión.

\textbf{1) Inicio del flujo de autorización.} Cuando el usuario pulsa «Conectar con Trakt», la aplicación llama a una ruta interna de inicio que calcula el \texttt{origin} activo (local o producción), construye el \texttt{redirect\_uri} correcto y genera un \texttt{state} con \textit{nonce} y ruta de retorno. Después redirige al usuario a la URL oficial de autorización de Trakt. Antes de redirigir, guarda en cookies \texttt{httpOnly} temporales el \texttt{state}, la ruta a la que debe volver el usuario y el \texttt{redirect\_uri} exacto usado en la solicitud.

\textbf{2) Retorno desde Trakt e intercambio del código por sesión.} Tras autorizar en Trakt, el proveedor redirige al callback interno de la aplicación con \texttt{code} y \texttt{state}. En esta fase el servidor valida el estado recibido frente al almacenado y si es correcto intercambia el código por \texttt{access\_token} y \texttt{refresh\_token} mediante una llamada servidor-a-servidor a \texttt{https://api.trakt.tv/oauth/token}. Con los tokens recibidos, el backend crea cookies \texttt{httpOnly} (token de acceso, token de refresco y fecha de expiración) y limpia las cookies temporales del flujo OAuth.

\textbf{3) Verificación de sesión.} Para saber si el usuario está conectado, el frontend consulta un endpoint de estado. Ese endpoint lee las cookies, comprueba si el token sigue vigente y valida la sesión contra Trakt con una petición de comprobación de usuario. Si detecta token inválido o revocado, limpia la sesión local de forma controlada.

\textbf{4) Renovación automática de tokens.} Además del endpoint de estado, el proyecto incluye un endpoint específico de refresco y un \textit{hook} de cliente que mantiene los tokens sincronizados. El hook calcula caducidad con margen de seguridad, solicita renovación cuando corresponde y actualiza su persistencia local para que la experiencia de uso no se interrumpa durante la navegación.

\textbf{5) Desconexión.} Al cerrar sesión de Trakt, la aplicación elimina las cookies de autenticación del lado servidor y limpia el estado de conexión del cliente.

Con este diseño, la gestión real del intercambio OAuth permanece en el servidor y el navegador nunca accede directamente a credenciales privadas y solo consume un estado de autenticación ya procesado por el backend.

% -----------------------------------------------------------------------
\section{Integración con las APIs externas}
% -----------------------------------------------------------------------

\subsection{The Movie Database (TMDb) API v3}

El cliente de TMDb (\texttt{lib/api/tmdb.js}) encapsula toda la lógica de acceso a la API v3. La función central \texttt{tmdb(path, params, options)} implementa:

\begin{itemize}
\item Un \textbf{constructor de URL unificado} que añade automáticamente la API Key y el idioma por defecto (\texttt{es-ES}).
\item Un \textbf{mecanismo de timeout} mediante \texttt{AbortController} (8 segundos por defecto) para evitar que peticiones lentas bloqueen la interfaz.
\item \textbf{Estrategia de caché}: en servidor se usa \texttt{force-cache} con revalidación ISR para reutilizar respuestas y reducir llamadas repetidas, mientras que en cliente se emplea \texttt{no-store} para evitar datos obsoletos en interacciones en tiempo real.
\item \textbf{Gestión granular de errores}: cuando la API devuelve un 404, la función interpreta que el recurso no existe y retorna \texttt{null} sin registrar un error. En el resto de casos, el fallo sí se registra en logs para diagnóstico y la función retorna igualmente \texttt{null} para no bloquear la interfaz.
\end{itemize}

Sobre esta función base se construyen más de 40 funciones nombradas por dominio (\texttt{fetchTopRatedMovies}, \texttt{getDetails}, \texttt{getCredits}, \texttt{getWatchProviders}, \texttt{getActorDetails}, etc.) que constituyen la API pública del módulo.

Un aspecto técnico importante es la resolución de imágenes: la función \texttt{getLogos} obtiene el logo oficial de un título aplicando un criterio de selección que prioriza logos en español, luego en inglés, luego sin idioma, seleccionando el de mayor número de votos dentro de cada grupo.

La Tabla~\ref{tab:tmdb-endpoints} recoge los \textit{endpoints} principales utilizados.

\begin{table}[!htbp]
\begin{center}
\begin{tabular}{| l | l |}
\hline
\textbf{Función} & \textbf{Endpoint TMDb} \\
\hline
\hline
\texttt{fetchTopRatedMovies()} & \texttt{GET /movie/top\_rated} \\
\hline
\texttt{fetchTrendingMovies()} & \texttt{GET /trending/movie/week} \\
\hline
\texttt{getDetails(type, id)} & \parbox[t]{0.62\linewidth}{\raggedright\strut\texttt{GET /\{type\}/\{id\}?}\\[2pt]\texttt{append\_to\_response=external\_ids}\strut} \\
\hline
\texttt{getLogos(type, id)} & \texttt{GET /\{type\}/\{id\}/images} \\
\hline
\texttt{getCredits(type, id)} & \texttt{GET /\{type\}/\{id\}/credits} \\
\hline
\texttt{getWatchProviders(type, id)} & \texttt{GET /\{type\}/\{id\}/watch/providers} \\
\hline
\texttt{getRecommendations(type, id)} & \texttt{GET /\{type\}/\{id\}/recommendations} \\
\hline
\texttt{markAsFavorite(\{\ldots\})} & \texttt{POST /account/\{id\}/favorite} \\
\hline
\texttt{markInWatchlist(\{\ldots\})} & \texttt{POST /account/\{id\}/watchlist} \\
\hline
\texttt{fetchFavoritesForUser(\ldots)} & \texttt{GET /account/\{id\}/favorite/movies|tv} \\
\hline
\texttt{fetchWatchlistForUser(\ldots)} & \texttt{GET /account/\{id\}/watchlist/movies|tv} \\
\hline
\end{tabular}
\caption{Principales funciones del cliente TMDb y sus \textit{endpoints}}
\label{tab:tmdb-endpoints}
\end{center}
\end{table}

\subsection{OMDb API (Open Movie Database)}

OMDb es la única fuente pública que permite obtener el \textit{rating} de IMDb, el número de votos y las puntuaciones de Rotten Tomatoes y Metacritic por \texttt{imdbId}. Para proteger la API Key, todas las peticiones a OMDb pasan por la API Route interna \texttt{/api/omdb}.

El \textit{proxy} implementa un \textbf{sistema de deduplicación} (\texttt{inflight Map} global): si múltiples tarjetas solicitan simultáneamente el mismo \texttt{imdbId}, solo se realiza una petición real a OMDb. Las respuestas exitosas se cachean 24 horas en el proceso de Node.js; las fallidas, 60 segundos para reintentar pronto.

\subsection{Proxy de Trakt y transformación de datos}

Las API Routes de Trakt actúan como intermediario que resuelve dos problemas estructurales:

\textbf{Problema de identificadores.} El \textit{frontend} trabaja exclusivamente con \texttt{tmdbId}, mientras que muchos \textit{endpoints} de Trakt requieren sus propios identificadores (\texttt{traktId}, \texttt{slug}). Las API Routes realizan internamente la traducción mediante el \textit{endpoint} de búsqueda por ID externo de Trakt (\texttt{/search/tmdb/\{id\}?type=movie|show}).

\textbf{Problema de enriquecimiento de datos.} Las respuestas de Trakt incluyen datos de \textit{tracking} pero no imágenes. Las API Routes que devuelven listas de contenido (historial, favoritos, pendientes) realizan peticiones paralelas a TMDb para obtener las URLs de \textit{poster} y \textit{backdrop}, enriqueciendo la respuesta antes de enviarla al cliente.

La tabla~\ref{tab:trakt-endpoints} recoge los \textit{endpoints} de Trakt utilizados por módulo funcional.

\begin{table}[!htbp]
\begin{center}
\begin{tabular}{| l | l |}
\hline
\textbf{Endpoint Trakt} & \textbf{Descripción} \\
\hline
\hline
\texttt{GET /sync/history} & Historial completo de películas y episodios vistos \\
\hline
\texttt{POST /sync/history} & Registrar película/episodio como visto \\
\hline
\texttt{DELETE /sync/history} & Desmarcar como visto \\
\hline
\texttt{GET /sync/watched/shows} & Estado de visionado de todas las series \\
\hline
\texttt{GET /sync/watchlist} & Lista de pendientes del usuario \\
\hline
\texttt{POST /sync/ratings} & Añadir valoración (1--10) \\
\hline
\texttt{GET /users/me/stats} & Estadísticas globales del usuario \\
\hline
\texttt{GET /calendars/my/shows} & Próximos episodios de series seguidas \\
\hline
\texttt{GET /search/tmdb/\{id\}} & Búsqueda de item por ID de TMDb \\
\hline
\end{tabular}
\caption{Principales \textit{endpoints} de Trakt utilizados}
\label{tab:trakt-endpoints}
\end{center}
\end{table}

\subsection{JustWatch (API GraphQL no oficial)}

JustWatch no tiene una API oficial pública. El cliente (\texttt{lib/api/justwatch.js}) utiliza los mismos \textit{endpoints} GraphQL que la web de JustWatch, enviando peticiones \texttt{POST} a \texttt{https://apis.justwatch.com/graphql}.

Se utilizan dos \textit{queries} GraphQL:

\begin{enumerate}
\item \textbf{Búsqueda del título} (\texttt{GetSearchTitles}): recibe el título y tipo de contenido y devuelve el \texttt{nodeId} interno de JustWatch.
\item \textbf{Detalles con ofertas} (\texttt{GetTitleDetails}): recibe el \texttt{nodeId} y devuelve las ofertas de \textit{streaming} disponibles para España, incluyendo el tipo de monetización (\texttt{FLATRATE}, \texttt{RENT}, \texttt{BUY}), la URL directa al contenido en la plataforma y los datos del proveedor.
\end{enumerate}

La función principal \texttt{getStreamingProviders(title, type, year)} filtra las ofertas de tipo \texttt{FLATRATE} (suscripción), les asigna el logo de la plataforma mediante un mapeo estático a los logos de TMDb y devuelve la URL directa al contenido.

\subsection{Plex Media Server}

La integración con Plex permite a los usuarios que tienen una biblioteca local abrir directamente cualquier película o serie desde \textit{The Show Verse} en su servidor Plex, sin tener que buscar el contenido manualmente.

\textbf{Autenticación.} El módulo \texttt{lib/plex/auth.js} implementa dos modos:
\begin{itemize}
\item \textbf{Modo simple:} se configura \texttt{PLEX\_TOKEN} en variables de entorno y se usa directamente en cada petición.
\item \textbf{Modo JWT:} se configura una clave privada RSA o Ed25519 (\texttt{PLEX\_JWT\_PRIVATE\_KEY}). La aplicación firma JWTs con la clave privada, los intercambia por tokens de acceso en la API de Plex y los renueva automáticamente antes de su expiración. La deduplicación de refrescos concurrentes se gestiona mediante un \texttt{Promise} compartido (\texttt{state.refreshPromise}).
\end{itemize}

\textbf{API Route interna (\texttt{GET /api/plex}).} Recibe \texttt{title}, \texttt{type}, \texttt{year}, \texttt{imdbId} y \texttt{tmdbId} como parámetros y devuelve varios formatos de URL para el acceso al contenido desde diferentes plataformas:

\begin{itemize}
\item \texttt{plexWebUrl}: URL para la interfaz web de Plex en escritorio.
\item \texttt{plexUniversalUrl}: \texttt{https://watch.plex.tv/\{type\}/\{slug\}}, un enlace universal que abre la app nativa en iOS y Android.
\item \texttt{plexSlugUrl}: \texttt{plex://\{type\}/\{slug\}}, \textit{deep link} nativo de la aplicación.
\item \texttt{plexAndroidSlugIntentUrl}: formato \texttt{intent://} para Chrome en Android.
\end{itemize}

El slug para las URLs universales se resuelve consultando \texttt{https://metadata.provider.plex.tv/library/metadata/matches?guid=imdb://\{imdbId\}}.

% -----------------------------------------------------------------------
\section{Arquitectura de API Routes propias}
% -----------------------------------------------------------------------

Next.js permite definir API Routes como funciones serverless que se ejecutan en el servidor, proporcionando endpoints HTTP personalizados. El proyecto implementa 74 API Routes organizadas en el directorio \texttt{src/app/api/}, que actúan como capa intermedia entre el cliente y las APIs externas, implementando lógica de negocio, transformación de datos, caché y seguridad.

\subsection{Funciones principales de las API Routes}

Las API Routes del proyecto cumplen seis funciones fundamentales:

\textbf{1. Proxy seguro.} Protegen las claves API (API keys) de exposición al navegador. Todas las peticiones a TMDb, Trakt, OMDb, Plex y JustWatch pasan por endpoints intermedios que almacenan las credenciales en variables de entorno del servidor, garantizando que nunca se envíen al cliente.

\textbf{2. Transformación y enriquecimiento de datos.} Las respuestas de las APIs externas se procesan antes de enviarlas al cliente: se traducen identificadores entre sistemas (TMDb ID, Trakt ID, IMDb ID), se combinan datos de múltiples fuentes en una única respuesta, y se añaden metadatos adicionales como títulos, posters y años de estreno desde TMDb.

\textbf{3. Caché y optimización.} Implementan caché en memoria para reducir llamadas redundantes a APIs externas con límites de tasa. Las respuestas exitosas se almacenan temporalmente (de 60 segundos a 24 horas según el tipo de dato) y se comparten entre peticiones concurrentes mediante deduplicación.

\textbf{4. Gestión de autenticación.} Manejan flujos OAuth 2.0 completos para Trakt y TMDb, almacenando tokens de sesión en cookies HTTP-only inaccesibles desde JavaScript. Implementan renovación automática de tokens caducados y validación de estado de conexión.

\textbf{5. Agregación de datos.} Consultan múltiples APIs en paralelo y combinan los resultados. Por ejemplo, \texttt{/api/tv/[id]/ratings} obtiene puntuaciones de TMDb, Trakt e IMDb simultáneamente y las devuelve en un objeto unificado.

\textbf{6. Validación y normalización.} Validan parámetros de entrada, normalizan formatos (ej: ratings de 1--10 de Trakt se redondean hacia arriba cuando provienen de TMDb que acepta decimales) y gestionan timeouts configurables para evitar bloqueos.

\subsection{Categorías funcionales de endpoints}

\subsubsection{Autenticación y gestión de sesiones}

\textbf{Flujo OAuth 2.0 con Trakt.} Se implementa mediante cuatro endpoints complementarios:

\begin{itemize}
\item \texttt{GET /api/trakt/auth/start}: Inicia el flujo OAuth generando la URL de autorización de Trakt con los parámetros \texttt{client\_id}, \texttt{redirect\_uri} y \texttt{state} (token CSRF para prevenir ataques). Maneja dinámicamente la URL de redirección según el entorno (Vercel en producción o localhost en desarrollo).

\item \texttt{GET /api/trakt/auth/callback}: Recibe el código de autorización tras la aprobación del usuario, valida el token CSRF, intercambia el código por \texttt{access\_token} y \texttt{refresh\_token} mediante petición servidor-a-servidor a Trakt, y almacena ambos tokens en cookies \texttt{httpOnly} con configuración \texttt{secure} y \texttt{sameSite}.

\item \texttt{GET /api/trakt/auth/status}: Verifica el estado de conexión consultando el endpoint \texttt{/users/settings} de Trakt. Si el token ha caducado, intenta renovarlo automáticamente usando el \texttt{refresh\_token} antes de declarar al usuario como desconectado.

\item \texttt{POST /api/trakt/auth/disconnect}: Revoca el token en Trakt haciendo uso de \texttt{/oauth/revoke} y elimina las cookies de sesión, completando el cierre de sesión.
\end{itemize}

\textbf{Autenticación TMDb.} El flujo es similar pero utiliza un sistema de request token:

\begin{itemize}
\item \texttt{GET /api/tmdb/auth/request-token}: Obtiene un token temporal que el usuario debe aprobar en la web de TMDb.

\item \texttt{POST /api/tmdb/auth/session}: Tras la aprobación, crea un \texttt{session\_id} persistente y lo almacena en cookie. Implementa reintentos (hasta 4 intentos con backoff) para compensar la latencia de propagación de la aprobación en los servidores de TMDb.

\item \texttt{GET /api/tmdb/auth/account}: Retorna los datos de la cuenta autenticada (username, avatar, configuración regional).
\end{itemize}

\subsubsection{Datos de usuario y actividad}

\textbf{Historial de visualización.} \texttt{GET /api/trakt/history} es el endpoint principal que obtiene el historial completo de películas y episodios vistos por el usuario. Acepta parámetros de filtrado por tipo (\texttt{movies}, \texttt{shows}, \texttt{all}), rango de fechas (\texttt{from} y \texttt{to} en formato YYYY-MM-DD), y paginación (\texttt{page}, \texttt{limit}). 

El proceso para obtener los datos se realiza primero consultando \texttt{/sync/history} de Trakt obteniendo identificadores y timestamps, luego se extraen todos los TMDb IDs únicos, y finalmente se ejecutan peticiones paralelas a TMDb (máximo 8 concurrentes) para obtener títulos, posters, años de estreno y sinopsis. Los resultados se agregan con estadísticas calculadas (total de visualizaciones por título, fecha de primer y último visionado).

Los endpoints complementarios permiten una gestión detallada del historial:
\begin{itemize}
\item \texttt{POST /api/trakt/history/add}: añade visualizaciones manuales con timestamp personalizado.
\item \texttt{POST /api/trakt/history/update}: modifica la fecha de una entrada existente.
\item \texttt{DELETE /api/trakt/history/remove}: elimina registros específicos mediante \texttt{historyId}.
\end{itemize}

\textbf{Progreso y estado de series.} \texttt{GET /api/trakt/show/watched} retorna un objeto estructurado que representa el estado de visionado completo de una serie: \texttt{\{watchedBySeason: \{``1'': [1,2,3,5], ``2'': [1,2]\}\}} indica que en la temporada 1 se han visto los episodios 1, 2, 3 y 5, mientras que en la temporada 2 solo los episodios 1 y 2.

\texttt{POST /api/trakt/show/plays} implementa marcado masivo de episodios, aceptando arrays con cientos de entradas en el formato \texttt{\{season, number, watchedAt\}}. Para respetar los límites de la API de Trakt, divide automáticamente las peticiones en chunks de 350 episodios máximo por llamada, y valida los números de episodio contra los datos canónicos de TMDb para prevenir marcado de episodios inexistentes.

\texttt{GET /api/trakt/item/status} proporciona un resumen completo del estado de cualquier título: si ha sido visto, cuántas veces se ha reproducido (\texttt{plays}), cuándo fue la última visualización, y el historial completo de fechas. Además, resuelve y devuelve la URL canónica del título en Trakt para acceso directo.

\textbf{Estadísticas de usuario.} \texttt{GET /api/trakt/user-stats} agrega datos de múltiples endpoints de Trakt (\texttt{/users/me/stats}, \texttt{/sync/history}, \texttt{/sync/watched}) y los enriquece con información de TMDb y créditos:

\begin{itemize}
\item Calcula el top de películas y series más vistas (por número de reproducciones).
\item Identifica actores y directores más frecuentes en el historial analizando los créditos de cada título.
\item Agrupa por géneros consumidos con distribución porcentual.
\item Suma el tiempo total de visionado en minutos a partir de la duración de cada episodio y película.
\item Genera gráficas de actividad mensual, distribución por día de la semana y hora del día.
\end{itemize}

\subsubsection{Valoraciones y ratings}

\textbf{Gestión de ratings personales.} \texttt{GET/POST /api/trakt/ratings} implementa CRUD completo de valoraciones del usuario con las siguientes características:

\begin{itemize}
\item \textbf{GET con \texttt{?type=all}}: Retorna todas las valoraciones (películas y series) del usuario autenticado.
\item \textbf{POST con \texttt{\{type, tmdbId, rating\}}}: Crea o actualiza una valoración. El rating se normaliza a escala 1--10 y se redondea hacia arriba si proviene de TMDb (que acepta decimales) para cumplir con las restricciones de Trakt (enteros únicamente).
\item \textbf{POST con \texttt{rating: null}}: Elimina la valoración existente del título, equivalente a DELETE.
\item Implementa auto-refresh de tokens: si detecta un token caducado, ejecuta el flujo de renovación automáticamente y reintenta la petición original.
\end{itemize}

El endpoint paralelo \texttt{POST /api/tmdb/movies/[id]/rating} gestiona valoraciones en TMDb con escala de 0.5 a 10 en incrementos de 0.5, permitiendo mayor granularidad que Trakt.

\textbf{Ratings agregados de múltiples fuentes.} \texttt{GET /api/tv/[id]/ratings} es un endpoint de agregación especializado para series que combina datos de tres fuentes:

\begin{enumerate}
\item Obtiene el rating promedio de TMDb directamente desde \texttt{/tv/\{id\}}.
\item Consulta el scoreboard de Trakt mediante \texttt{/search/tmdb/\{id\}} para obtener la valoración comunitaria.
\item Extrae el IMDb ID de los identificadores externos de TMDb, y consulta OMDb para obtener el rating oficial de IMDb, el número de votos, y puntuaciones de Rotten Tomatoes y Metacritic.
\end{enumerate}

Las tres fuentes se consultan en paralelo mediante \texttt{Promise.allSettled()}, garantizando que un fallo en cualquiera de ellas no bloquee las demás. El resultado se estructura como \texttt{\{tmdb: \{rating, votes\}, trakt: \{rating, votes\}, imdb: \{rating, votes, rtScore, mcScore\}\}}.

\subsubsection{Proxy y caché de datos externos}

\textbf{Proxy OMDb con caché optimizada.} \texttt{GET /api/omdb} implementa un sistema de caché completo con las siguientes capas:

\begin{itemize}
\item \textbf{Caché en memoria}: \texttt{Map} global que almacena respuestas exitosas durante 24 horas y respuestas fallidas (404, timeouts) durante 60 segundos. El TTL corto para fallos permite reintentos rápidos sin saturar la API.

\item \textbf{Deduplicación de peticiones concurrentes}: Mantiene un \texttt{Map} de promesas en vuelo (\texttt{inflightRequests}). Si 10 tarjetas solicitan simultáneamente el mismo IMDb ID, solo se ejecuta una petición HTTP real y todas comparten el resultado, reduciendo el tráfico en un factor de 10x durante cargas iniciales de listas.

\item \textbf{Timeout configurable}: Acepta parámetro \texttt{timeoutMs} (800--20000ms) para ajustar el tiempo máximo de espera según el contexto (búsquedas rápidas vs. cargas completas).

\item \textbf{Validación de respuesta}: Verifica que la respuesta sea JSON válido antes de cachearla, descartando errores HTML de OMDb.
\end{itemize}

\textbf{Imágenes localizadas en batch.} \texttt{POST /api/tmdb/localized-images} optimiza la obtención de posters y backdrops para decenas de títulos simultáneamente:

\begin{itemize}
\item Acepta arrays de IDs en el body: \texttt{\{type: ``movie'', ids: [1,2,3,...], kind: ``poster''\}}.
\item Ejecuta peticiones con pool de concurrencia limitado a 8 workers para respetar rate limits.
\item Implementa selección inteligente de imágenes: prioriza backdrops $\geq$1280px de ancho y posters $\geq$500px, prefiere contenido en inglés o sin texto superpuesto (\texttt{iso\_639\_1 === null}), y ordena por número de votos.
\item Retorna objeto indexado por ID: \texttt{\{``123'': \{poster\_path, backdrop\_path\}, ``456'': \{...\}\}}.
\end{itemize}

\subsubsection{Integración con Plex Media Server}

\textbf{Búsqueda multi-servidor.} \texttt{GET /api/plex} implementa búsqueda resiliente en múltiples servidores Plex configurados:

\begin{itemize}
\item Acepta parámetros de búsqueda: \texttt{title}, \texttt{type} (movie|tv), \texttt{year}, \texttt{imdbId}, \texttt{tmdbId}.
\item Itera sobre todas las URLs de servidor configuradas en \texttt{PLEX\_URL} (separadas por coma), probando primero HTTPS y haciendo fallback a HTTP para localhost.
\item Implementa timeout de 8 segundos por servidor para evitar bloqueos.
\item Realiza matching multinivel: primero por IMDb ID o TMDb ID si están disponibles en los \texttt{Guid} de Plex, luego por coincidencia de título normalizado (case-insensitive, sin acentos) y año con tolerancia de ±1 año.
\item Retorna el primer resultado encontrado con metadatos completos: \texttt{\{found: true, items: [...], server: ``url''\}}.
\end{itemize}

\textbf{Biblioteca completa.} \texttt{GET /api/plex/library} lista todo el contenido disponible:

\begin{itemize}
\item Implementa paginación interna (200 ítems por página) para manejar bibliotecas con miles de títulos.
\item Extrae metadatos técnicos: resolución (4K, 1080p, 720p, SD), códec de vídeo (H.264, HEVC, AV1), contenedor (mkv, mp4), tamaño en disco, número de pistas de audio y subtítulos.
\item Ordena por resolución descendente (8K $\rightarrow$ 4K $\rightarrow$ 1080p $\rightarrow$ SD) y elimina duplicados por título.
\item Limita resultados a 10000 ítems máximo para prevenir sobrecarga de memoria.
\end{itemize}

\subsubsection{Enlaces externos y descubrimiento}

\textbf{Generación inteligente de enlaces.} Los endpoints de enlace implementan estrategias de búsqueda y caché:

\texttt{GET /api/links/letterboxd} genera URLs a Letterboxd mediante dos métodos:
\begin{enumerate}
\item Si el título tiene IMDb ID disponible, redirige a \texttt{/imdb/tt1234567} (URL estable que Letterboxd resuelve automáticamente).
\item Si solo tiene título y año, construye un slug normalizado (\texttt{inception-2010}), hace scraping de la página HTML de Letterboxd para extraer la URL final desde los metadatos Open Graph, y cachea el resultado 24 horas.
\end{enumerate}

\texttt{GET /api/links/justwatch} realiza búsqueda real en JustWatch.com:
\begin{enumerate}
\item Consulta la página de búsqueda de JustWatch con el título.
\item Extrae el JSON embebido en \texttt{\_\_NEXT\_DATA\_\_} que contiene los resultados indexados.
\item Hace matching por año (si está disponible) para desambiguar títulos homónimos.
\item Fallback a extracción desde metadatos \texttt{og:url} o \texttt{<link rel=``canonical''>} si la estructura del JSON cambia.
\item Retorna \texttt{\{url: ``https://www.justwatch.com/es/pelicula/inception''\}}.
\end{enumerate}

\textbf{Descubrimiento y recomendaciones.} \texttt{GET /api/trakt/discover/recommend} implementa recomendaciones públicas:

\begin{itemize}
\item Consulta \texttt{/movies/recommended} y \texttt{/shows/recommended} de Trakt en paralelo.
\item Intercala resultados (película, serie, película, serie, ...) para variedad.
\item Enriquece cada item con datos de TMDb (título localizado, poster, fecha de estreno, sinopsis).
\item Limita a 24 items totales para mantener tiempos de respuesta bajo 2 segundos.
\end{itemize}

\texttt{GET /api/trakt/related} obtiene contenido similar a un título específico:
\begin{enumerate}
\item Traduce el TMDb ID al slug de Trakt mediante \texttt{/search/tmdb/\{id\}}.
\item Consulta \texttt{/\{type\}/\{slug\}/related?extended=full\&limit=15}.
\item Enriquece los resultados con posters y backdrops de TMDb usando \texttt{mapWithConcurrency} para limitar concurrencia.
\item Retorna hasta 15 títulos relacionados ordenados por similitud según el algoritmo de Trakt.
\end{enumerate}

\subsubsection{Listas y colecciones}

\textbf{Listas personales de Trakt.} \texttt{GET /api/trakt/lists} obtiene todas las listas del usuario autenticado:

\begin{itemize}
\item Consulta \texttt{/users/me/lists} obteniendo metadatos (nombre, descripción, privacidad, número de ítems, fecha de creación).
\item Para cada lista, consulta \texttt{/users/me/lists/\{id\}/items} obteniendo los primeros 48 ítems.
\item Enriquece cada ítem con título, poster y año de TMDb mediante peticiones paralelas.
\item Implementa manejo de rate limits: si detecta código 429, espera el tiempo indicado en \texttt{Retry-After} y reintenta.
\item Elimina duplicados que puedan existir en las listas de Trakt.
\end{itemize}

\texttt{GET /api/trakt/community/lists} obtiene listas públicas que contienen un título específico:

\begin{itemize}
\item Acepta parámetros: \texttt{type}, \texttt{tmdbId}, \texttt{tab} (popular|trending|recent), \texttt{page}, \texttt{limit}.
\item Traduce el TMDb ID al slug de Trakt y consulta \texttt{/\{type\}/\{slug\}/lists/\{tab\}}.
\item Retorna listas con metadatos del creador, número de likes, número de comentarios, y muestra los primeros 4 posters de cada lista para preview.
\end{itemize}

\textbf{Colecciones de TMDb.} \texttt{GET /api/tmdb/collections/search} y \texttt{/api/tmdb/collections/featured} proporcionan acceso a sagas y franquicias:

\begin{itemize}
\item \texttt{search} acepta parámetro \texttt{query} y retorna colecciones con \texttt{item\_count} (número de películas en la saga).
\item \texttt{featured} retorna colecciones curadas por popularidad (MCU, Star Wars, Harry Potter, etc.).
\item \texttt{GET /api/tmdb/collection} con ID retorna detalles completos incluyendo todas las películas en orden cronológico.
\end{itemize}

\subsection{Patrones técnicos transversales}

Todos los endpoints implementan patrones comunes de robustez:

\textbf{Manejo de errores estructurado.} Las respuestas de error siguen un formato consistente: \texttt{\{error: ``descripción'', code?: ``ERR\_CODE'', details?: \{...\}\}}. Los códigos HTTP se asignan semánticamente: 400 para parámetros inválidos, 401 para autenticación requerida, 404 para recursos no encontrados, 429 para rate limit, 500 para errores internos, 503 para APIs externas no disponibles.

\textbf{Validación de entrada.} Los parámetros se validan antes de ejecutar lógica costosa: TMDb IDs deben ser numéricos positivos, fechas deben cumplir formato ISO 8601, ratings se restringen a rangos válidos (1--10), y se rechazan requests con tamaños excesivos (límite de 10000 IDs en batch queries).

\textbf{Timeouts y circuit breakers.} Todas las peticiones a APIs externas incluyen timeouts configurables (valores típicos: 5s para datos críticos, 10s para búsquedas, 15s para operaciones batch). Si un servicio externo falla repetidamente, se activa degradación graceful retornando datos parciales o cacheados.

\textbf{Logging y observabilidad.} Los errores se loguean con contexto completo (endpoint, parámetros, usuario si está autenticado, timestamp, stack trace) permitiendo debugging en producción. Las peticiones lentas ($>$3 segundos) se marcan como warnings para identificar cuellos de botella.

% -----------------------------------------------------------------------
\section{Implementación de los módulos funcionales}
% -----------------------------------------------------------------------

En esta sección se documentan en profundidad los módulos principales de la aplicación, explicando su arquitectura, flujos de datos, patrones de optimización y características funcionales.

\subsection{Dashboard principal (\texttt{/})}

Es el punto de entrada de la aplicación y muestra un conjunto de secciones filtradas y organizadas por temas.

La página de inicio (\texttt{src/app/page.jsx}) es un Server Component que ejecuta en paralelo todas las peticiones necesarias de TMDb mediante \texttt{Promise.all()} lo que permite reducir el tiempo total de carga a la latencia de la petición más lenta.

A continuación se muestran las secciones principales del dashboard, su respectivo endpoint de TMDb y el resultado obtenido:

\begin{table}[!htbp]
\begin{center}
\small
\begin{tabular}{| l | l | l |}
\hline
\textbf{Sección} & \textbf{Endpoint TMDb} & \textbf{Descripción} \\
\hline
\hline
Hero & \texttt{/trending/movie/week} & Carrusel de películas de este mes \\
\hline
Top Películas & \texttt{/movie/top\_rated} & Películas mejor valoradas de todos los tiempos \\
\hline
Tendencias & \texttt{/trending/movie/week} & Películas populares esta semana \\
\hline
Populares & \texttt{/movie/popular} & Contenido con mayor momentum \\
\hline
Acción & \texttt{/discover/movie?with\_genres=28} & Películas de acción clásicas \\
\hline
Dramas & \texttt{/discover/movie?with\_genres=18} & Películas dramáticas imprescindibles \\
\hline
Series Populares & \texttt{/tv/popular} & Series trending del momento \\
\hline
Series Top Rated & \texttt{/tv/top\_rated} & Series clásicas mejor valoradas \\
\hline
Series Trending & \texttt{/trending/tv/week} & Series en tendencia esta semana \\
\hline
Emitiéndose Hoy & \texttt{/tv/airing\_today} & Episodios con emisión real-time \\
\hline
En Emisión & \texttt{/tv/on\_the\_air} & Series con temporada activa \\
\hline
Favoritos (auth) & \texttt{/account/\{id\}/favorite/movies} & Favoritos personales del usuario \\
\hline
\end{tabular}
\caption{Secciones del dashboard y sus fuentes de datos}
\label{tab:dashboard-sections}
\end{center}
\end{table}

\textbf{Selección de backdrops:} el carrusel principal utiliza \textit{backdrops} de TMDb y se aplica un criterio de selección a partir de la función \texttt{pickBestBackdropByLangResVotes(...)} que prioriza primero los backdrops en inglés (\texttt{en}, \texttt{en-US}) cuando existen, después filtra por resolución y por \texttt{vote\_count} y \texttt{vote\_average}.


\subsection{Página de detalles (\texttt{/details/[type]/[id]})}

La página de detalles es el componente más complejo de la aplicación. 
Su funcionalidad es mostrar la información completa de la película o serie seleccionada a partir de los datos de TMDb, Trakt.tv, OMDb (IMDb), Plex Media Server, JustWatch, Letterboxd y servicios adicionales. Además, permite al usuario interactuar con el contenido (marcar como favorito, añadir a pendientes, valorar, compartir, etc.) y navegar por secciones relacionadas (reparto, recomendaciones, colecciones).

\subsubsection{Arquitectura de datos}

Se hace uso de Server Component y Client Component que optimiza tanto el SEO como la interacción en la página web. El Server Component se encarga de obtener los datos críticos iniciales en tiempo de renderizado \textit{server-side}, incluyendo los metadatos completos del título, los logos oficiales, el reparto y equipo técnico completo, las recomendaciones de contenido similar, los proveedores de \textit{streaming} disponibles por país, los videos oficiales (trailers, teasers, clips) y las reseñas de la comunidad. Si el usuario está autenticado, también se obtiene el estado de la cuenta (si ha marcado el título como favorito, si está en su lista de pendientes, y su valoración personal).

Una vez que el Client Component se ejecuta en el navegador, se empiezan a realizar peticiones paralelas para ir construyendo la interfaz. Primero se carga el scoreboard de Trakt mediante \texttt{/api/trakt/scoreboard} para obtener la puntuación, el número de usuarios que están viéndolo actualmente (\texttt{watchers}), el número total de reproducciones (\texttt{plays}), cuántos usuarios lo tienen en sus bibliotecas (\texttt{collectors}) y cuántas listas de Trakt lo incluyen. En paralelo, si el título tiene IMDb ID, se consulta OMDb mediante \texttt{/api/omdb} para obtener el rating de IMDb, el número de votos, las puntuaciones de Rotten Tomatoes y Metacritic, y los premios ganados. Todos estos datos se guardan en caché: OMDb en \texttt{sessionStorage} con TTL de 24 horas, y el scoreboard de Trakt en memoria con TTL de 5 minutos.

A partir de la API de Plex se detecta si el título está disponible en la biblioteca personal y se guarda durante 7 días en \texttt{sessionStorage} y se generan múltiples URLs de reproducción: una para el navegador web (\texttt{plexUrl.web}) y otra para la aplicación móvil de Plex (\texttt{plexUrl.mobile}).

Los proveedores de \textit{streaming} se obtienen de dos fuentes distintas. Por un lado, TMDb proporciona información básica de la disponibilidad mediante \texttt{watch/providers}, indicando si el título está disponible para alquiler, compra o en plataformas de suscripción. Por otro lado, JustWatch (mediante la API no oficial en GraphQL) proporciona enlaces directos al contenido en cada plataforma. 
Si la película pertenece a una colección (por ejemplo, la saga de \textit{El Señor de los Anillos}), se carga la información completa de la colección mediante \texttt{/api/tmdb/collection} y se muestra una nueva sección con un carrusel y todas las películas de la saga, ordenadas cronológicamente.

\subsubsection{Menú de navegación por secciones}

El componente \texttt{DetailsSectionMenu.jsx} implementa un menú de navegación con pestañas que se adapta dinámicamente en función del número de secciones disponibles que haya y el dispositivo cliente. Para películas, el menú incluye las secciones Media (galería de imágenes y videos), Sentimientos (análisis de pros y contras extraído de comentarios de Trakt mediante IA), Comentarios (reviews de TMDb y comentarios de Trakt), Listas (listas de Trakt que incluyen el título, con pestañas Popular y Trending), Colección (si la película pertenece a una saga), Reparto (reparto completo con fotos y personajes) y Recomendaciones (títulos similares sugeridos por el algoritmo de TMDb y Trakt). Para las series, se añaden las secciones Temporadas y Episodios ya que son exclusivas para este tipo.

\subsubsection{Sistema de puntuaciones}

El componente \texttt{ScoreboardBar.jsx} centraliza las puntuaciones de todas las plataformas en una barra horizontal. Aquí se muestran las puntuaciones de TMDb, Trakt, IMDb (OMDb), Rotten Tomatoes (OMDb) y Metacritic (OMDb) si están disponibles y cada una de ellas se muestra dentro de un badge con el logo de la plataforma, el valor numérico, el número de votos y el enlace externo a la página del título correspondiente de cada plataforma.

La puntuación de IMDb se obtiene de OMDb (\texttt{/api/omdb?imdb=\{imdbId\}}) y se muestra junto con el número de votos. Esta petición solo se realiza si el título tiene asociado un IMDb ID en los \texttt{external\_ids} de TMDb, y el resultado se cachea en \texttt{sessionStorage} con una clave específica \texttt{omdb:\{imdbId\}} y un TTL de 24 horas. OMDb también devuelve puntuaciones de Rotten Tomatoes y Metacritic (campos \texttt{RottenTomatoes} y \texttt{Metascore}).

Además de las puntuaciones, el scoreboard muestra estadísticas de Trakt mediante (\texttt{MiniStat}) que incluye iconos descriptivos: el número de usuarios que están viendo el título en este momento (\texttt{watchers}, icono Eye), el número total de reproducciones históricas (\texttt{plays}, icono Play), cuántos usuarios lo tienen en sus bibliotecas (\texttt{collectors}, icono LibraryBig), cuántos comentarios ha recibido (\texttt{comments}, icono MessageSquare) y en cuántas listas públicas de Trakt está incluido (\texttt{lists}, icono List). Estas estadísticas se cargan mediante \texttt{requestIdleCallback} para no bloquear el renderizado inicial del contenido mas relevante.

\subsubsection{Botones de acción}

La página de detalles incluye varios botones que permiten al usuario interactuar con su contenido.

\textbf{Botón de Favoritos.} Este botón permite marcar películas o series como favoritas en la cuenta de TMDb del usuario. Utiliza la función \texttt{markAsFavorite(\{accountId, sessionId, type, mediaId, favorite\})} que realiza una petición POST al endpoint de TMDb \texttt{/account/\{id\}/favorite}. Cuando el usuario hace clic en el botón, el icono cambia inmediatamente de un corazón vacío a un corazón relleno.

\textbf{Botón de Pendientes.} Similar al botón de Favoritos, este botón añade o elimina contenido de la lista de pendientes (watchlist) del usuario en TMDb. Funciona mediante la misma API de TMDb pero utilizando el endpoint \texttt{/account/\{id\}/watchlist} con una petición POST. El icono alterna entre un marcador vacío y un marcador relleno según el estado actual y la lista sirve para que el usuario organice el contenido que desea ver próximamente.

\textbf{Botón de Valoración personal.} Este botón muestra una ventana emergente que permite al usuario poner una puntuación personal del 1 al 10 a la película o serie. La valoración se envía simultáneamente a TMDb y Trakt mediante peticiones POST, almacenándose en ambas plataformas. Cuando el usuario ya tiene una valoración previa guardada, ésta se muestra en la interfaz y aparece un botón de papelera que permite eliminarla mediante la función \texttt{clearTmdbRating()}, esto ejecuta peticiones DELETE tanto a TMDb como a Trakt para mantener ambas cuentas sincronizadas. El sistema implementa sincronización bidireccional: si el usuario modifica su puntuación en una plataforma, el cambio se aplica automáticamente a la otra mediante \texttt{sendTraktRating(value)}, garantizando que ambos perfiles reflejen siempre la misma valoración. TMDb acepta puntuaciones con decimales mientras que Trakt requiere valores enteros, por lo que el sistema redondea hacia arriba automáticamente cuando es necesario.

\textbf{Botón de Listas personalizadas.} Exclusivo para películas, este botón abre el componente \texttt{AddToListModal} que permite gestionar las listas personales del usuario en TMDb. El modal carga todas las listas existentes consultando \texttt{GET /account/\{id\}/lists} y verifica en cuáles de ellas está incluida la película mediante peticiones a \texttt{GET /list/\{listId\}/item\_status?movie\_id=\{id\}}. El resultado se muestra como una lista vertical con casillas de verificación que indican si la película pertenece o no a cada lista y el usuario puede añadir o quitar la película de cualquiera de ellas. También existe la opción de crear una nueva lista directamente desde este modal rellenando un formulario con nombre y descripción.

\textbf{Botón de Marcar como visto (series).} Exclusivo para series, este botón gestiona el seguimiento de episodios vistos haciendo uso de la API de Trakt. Cuando el usuario marca un episodio como visto, se ejecuta \texttt{toggleEpisodeWatched()} que envía una petición POST a \texttt{/api/trakt/episode/watched} incluyendo el identificador de la serie, número de temporada, número de episodio y la fecha/hora actual. Existen funcionalidades en este componente como el botón de marcar serie que envía todos los episodios no vistos en una única petición y marca la serie como completada en una acción.

\textbf{Botón de Compartir.} Este botón permite compartir el enlace de la página de detalles actual mediante las opciones nativas del navegador o del sistema operativo. Utiliza la API Web Share cuando está disponible (principalmente en dispositivos móviles) o copia el enlace al portapapeles como alternativa en navegadores de escritorio que no soporten esta funcionalidad.

\subsubsection{Gestión avanzada de episodios para series}

El seguimiento de episodios de una serie es una parte importante del proyecto. El estado de visionado se representa mediante un componente anidado \texttt{watchedBySeason} con la estructura \texttt{\{``seasonNumber'': \{``episodeNumber'': true/false\}\}}, que se construye a partir de la respuesta de Trakt \texttt{GET /sync/watched/shows} y se transforma por la API Route \texttt{/api/trakt/show/watched}. El componente \texttt{SeasonDetailsClient.jsx} renderiza cada temporada como una sección expandible que, al abrirse, carga los detalles de todos los episodios de esa temporada mediante \texttt{getSeasonDetails(showId, seasonNumber)} y los muestra en formato grid o lista según la preferencia del usuario (guardada en \texttt{localStorage}). Cada episodio se renderiza con su número, nombre, fecha de emisión, puntuación de TMDb, puntuación de Trakt (si está disponible), y un checkbox interactivo que permite marcar/desmarcar el episodio como visto de forma individual.
Cuando el usuario marca un episodio como visto, se ejecuta la función \texttt{toggleEpisodeWatched(seasonNumber, episodeNumber)} que realiza una actualización del checkbox y envía una petición POST a \texttt{/api/trakt/episode/watched} con el payload \texttt{\{showId, season, episode, watchedAt: new Date().toISOString()\}} y si tiene éxito, se recarga el estado completo de la serie mediante \texttt{GET /sync/watched/shows/\{id\}} para sincronizar con cualquier cambio que haya podido ocurrir en Trakt desde otros dispositivos o aplicaciones.

El componente \texttt{EpisodeRatingsGrid.jsx} ubicado en una sección de la página de detalles ofrece otro tipo de vista de todos los episodios de la serie. Cada celda del grid representa un episodio y tiene un color en función de la puntuación, es completamente interactivo y se puede hacer clic en cualquier celca para acceder a la página de detalles del episodio en cuestión (\texttt{/details/tv/\{showId\}/season/\{season\}/episode/\{number\}}).

Para optimizar el rendimiento en series con cientos de episodios, las puntuaciones se cargan en paralelo mediante \texttt{Promise.all(seasons.map(s => getSeasonDetails(showId, s.season\_number)))} en lugar de secuencialmente. Esto reduce la latencia total de aproximadamente N segundos (donde N es el número de temporadas).

\subsubsection{Enlaces externos y plataformas de streaming}

En la página de detalles se integran dos tipos de enlaces que se muestran de forma distinta según el modo de visualización y el dispositivo. Las plataformas de streaming (Netflix, Amazon Prime Video, HBO Max, Disney+, etc.) se muestran directamente debajo de la imagen principal ya sea backdrop o poster. Cuando el contenido está disponible en el servidor Plex personal del usuario, aparece el logo de Plex con un indicador verde que señala la disponibilidad local. Los enlaces externos a servicios de bases de datos y redes sociales (web oficial, TMDb, Trakt, IMDb, FilmAffinity, JustWatch, Letterboxd, SeriesGraph) tienen una ubicación adaptada según varios factores.

En la vista por defecto con poster vertical, los enlaces externos se integran en la barra de puntuaciones que aparece debajo de los botones de acción. En pantallas de escritorio, los iconos de cada servicio se muestran en línea con sus logos, alineados a la derecha de las puntuaciones de TMDb, Trakt e IMDb. En dispositivos móviles, debido al espacio horizontal limitado, se ocultan los iconos individuales y en su lugar se muestra un botón que abre el componente \texttt{ExternalLinksModal}. En el modo de vista previa con backdrop horizontal, los enlaces externos cambian completamente de ubicación y se muestran junto a las plataformas de streaming debajo de la imagen principal, formando una única fila con un separador vertical.

El sistema de resolución de enlaces implementa tres niveles de complejidad según la plataforma. Los enlaces directos a TMDb, Trakt e IMDb se construyen mediante el ID correspondiente sin necesidad de peticiones adicionales: TMDb utiliza el ID numérico junto con un slug del título (por ejemplo, \texttt{/movie/550-fight-club}), Trakt usa el ID obtenido durante la carga del scoreboard (campo \texttt{trakt.traktUrl}), e IMDb emplea el IMDb ID en formato \texttt{tt1234567}. El enlace a la web oficial del título se extrae directamente del campo \texttt{homepage} de TMDb o, si no está disponible, mediante una petición a la API interna \texttt{/api/trakt/official-site}. FilmAffinity, que carece de API pública, se resuelve mediante una URL de búsqueda construida dinámicamente con el título normalizado.

Los enlaces a JustWatch y Letterboxd requieren un proceso de validación. Para JustWatch, la API interna \texttt{/api/links/justwatch} recibe el título y el año, genera un slug normalizado eliminando caracteres especiales y convirtiendo espacios en guiones, construye la URL final \texttt{https://www.justwatch.com/es/pelicula/\{slug\}}, ejecuta una petición HEAD para verificar que el recurso existe (código HTTP 200), y cachea el resultado en \texttt{localStorage} de forma indefinida para evitar validaciones repetidas pero si la validación falla, devuelve una URL de búsqueda genérica como fallback. Para Letterboxd (exclusivo de películas), si el título tiene IMDb ID se construye directamente la URL \texttt{https://letterboxd.com/imdb/\{imdbId\}} y en caso contrario, se genera un slug basado en el título y año, se valida su existencia mediante petición HEAD, y se cachea el resultado. Por último, el enlace de SeriesGraph (exclusivo de series) utiliza directamente el TMDb ID sin validación, construyendo la URL \texttt{https://seriesgraph.com/show/\{tmdbId\}}.

\subsubsection{Imágenes y posters}

El sistema de gestión de imágenes implementa tres modos de visualización distintos y permite a los administradores seleccionar manualmente qué versión de poster o backdrop se muestra. Para series de televisión, el sistema carga automáticamente las imágenes de todas las temporadas mediante \texttt{fetchTVImages(\{showId, apiKey\})} que itera por cada temporada, obtiene sus imágenes mediante \texttt{GET /tv/\{id\}/season/\{seasonNumber\}/images}, y fusiona todos los resultados en un único array eliminando duplicados por \texttt{file\_path}.

El sistema distingue tres tipos de imágenes según su función visual. Los posters son imágenes verticales con aspect ratio 2:3 que se muestran en el hero de la página de detalle y se optimizan en dos calidades: \texttt{w342} para la carga inicial rápida y \texttt{w780} para la carga final de alta calidad. Los backdrops de preview son imágenes horizontales con aspect ratio 16:9, optimizadas en tamaños \texttt{w780} y \texttt{w1280}.

La selección automática de la mejor imagen se realiza mediante funciones. Para posters, \texttt{pickBestPosterTV(posters, languages)} ordena por resolución (preferencia por imágenes 4K y 2K) y número de votos, priorizando posters en español o inglés si están disponibles. Para backdrops, \texttt{pickBestBackdropTVNeutralFirst(backdrops, languages)} prioriza específicamente las imágenes con \texttt{iso\_639\_1 === null} (sin texto superpuesto) para el background de móvil, y luego ordena por resolución y votos para el resto.

Los administradores (específicamente el usuario \texttt{psantos26} hardcoded en el componente) tienen acceso a una galería administrable con tres pestañas: Posters, Backdrops (para modo preview) y Backgrounds (para fondo móvil). Cada pestaña incluye filtros de resolución (Todos, 720p, 1080p, 2K, 4K calculados a partir del \texttt{width} de cada imagen) y checkboxes de idioma (ES, EN). Las imágenes se muestran en un carrusel horizontal y al hacer clic en cualquier imagen se establece como activa, guardando la selección en \texttt{localStorage} con claves específicas por tipo e ID (por ejemplo, \texttt{showverse:movie:550:poster}, \texttt{showverse:tv:1399:backdrop}).

\begin{figure}[H]
  \centering
  \includegraphics[width=1\textwidth]{../notes/.images/portadas.jpg}
  \caption{Interfaz de gestión de portadas y fondos.}
  \label{fig:portadas}
\end{figure}

\subsubsection{Sección de reparto y ficha de personas}

La sección de reparto unifica datos de múltiples fuentes. Los directores y creadores se extraen del array \texttt{credits.crew} filtrando por \texttt{job === ``Director''} o \texttt{department === ``Writing''} y se agregan al inicio del array de cast para darles prioridad. El array completo se ordena por el campo \texttt{order} que TMDb proporciona para indicar el orden de importancia de cada actor en la producción, de forma que los protagonistas aparecen primero. Cada tarjeta de actor muestra la foto del perfil en tamaño \texttt{w185}, el nombre real del actor, el nombre del personaje que interpreta, y un efecto de hover. Hacer clic en cualquier actor navega a la página de ficha de persona (\texttt{/details/person/\{id\}}) que carga mediante el componente \texttt{ActorDetails.jsx}.

La ficha de persona muestra la biografía completa del actor (campo \texttt{biography} de TMDb), su foto de perfil, fecha y lugar de nacimiento, y una filmografía completa dividida en dos pestañas: películas y series de televisión. La filmografía se ordena cronológicamente por \texttt{release\_date} o \texttt{first\_air\_date} en orden descendente (trabajos más recientes primero), y cada entrada muestra el poster del título, el año, el nombre del personaje o rol, y el rating de TMDb. La página también incluye enlaces externos a IMDb (si el actor tiene \texttt{imdb\_id}), Instagram, Twitter, Facebook y sitio web oficial, construidos a partir del objeto \texttt{external\_ids} de TMDb.

\subsubsection{Comentarios, análisis de sentimientos y listas de Trakt}

La sección de comentarios fusiona reviews de TMDb con comentarios de Trakt en la misma sección. Los comentarios de Trakt se organizan en tres categorías: <<Top 30 días>> que muestra los comentarios con más likes recibidos en los últimos 30 días, <<Top histórico>> que muestra los comentarios con más likes de todos los tiempos ordenados descendentemente por \texttt{likes} y <<Más recientes>> que muestra todos los comentarios ordenados por fecha de creación en orden descendente. Cada comentario incluye el avatar del usuario, su nombre de usuario, la puntuación que dio al título si es que votó, una advertencia de spoiler si el comentario está marcado como tal, el número de likes que ha recibido, el número de respuestas, la fecha de publicación relativa y el texto completo del comentario.

La funcionalidad del análisis de sentimiento de Trakt se construye mediante la función \texttt{buildSentimentFromComments(items)} que procesa todos los comentarios cargados, extrae frases que contienen palabras clave positivas (como <<excelente>>, <<increíble>>, <<obra maestra>>, <<recomendable>>) o negativas (como <<decepcionante>>, <<aburrido>>, <<predecible>>, <<desperdicio>>), y genera dos listas: una de pros (aspectos positivos mencionados frecuentemente) y otra de contras (aspectos negativos recurrentes).

La sección de listas muestra las listas públicas de Trakt que incluyen este título, organizadas en dos pestañas: Popular (ordenadas por número de likes) y Trending (ordenadas por actividad reciente). Cada lista se renderiza como una tarjeta que muestra el nombre de la lista, una descripción, el nombre del usuario que la creó, el número de items que contiene, el número de likes que ha recibido y el número de comentarios. Hacer clic en cualquier lista navega a la página completa de esa lista en Trakt.

\subsubsection{Recomendaciones y contenido similar}

La sección de recomendaciones utiliza el algoritmo de Trakt para sugerir títulos relacionados mediante el endpoint \texttt{/\{type\}/\{slug\}/related?extended=full\&limit=15}, que analiza patrones de visionado de la comunidad, géneros compartidos, directores, actores y palabras clave para determinar similitud.
El proceso de obtención tiene tres fases secuenciales que transforman IDs. Primero, la API Route \texttt{/api/trakt/related} recibe el TMDb ID del título actual y lo traduce al slug de Trakt mediante el endpoint de búsqueda \texttt{GET /search/tmdb/\{id\}?type=movie|show}, que devuelve el objeto completo del título incluyendo todos sus identificadores (\texttt{ids.slug}, \texttt{ids.trakt}, \texttt{ids.imdb}, \texttt{ids.tmdb}). Con el slug de Trakt obtenido, se ejecuta la petición de contenido relacionado que devuelve un array de hasta 15 títulos similares con metadatos básicos pero sin imágenes. Finalmente, cada resultado se completa individualmente con datos de TMDb mediante peticiones paralelas (\texttt{mapWithConcurrency}) obteniendo \texttt{poster\_path}, \texttt{backdrop\_path}, \texttt{vote\_average}, \texttt{overview} y fecha de estreno.

\subsection{Favoritos (\texttt{/favorites})}

La página de favoritos muestra la colección de películas y series del usuario a partir de la API TMDb. El componente \texttt{FavoritesClient.jsx} obtiene los datos mediante dos peticiones paralelas a los \textit{endpoints} \texttt{GET /account/\{id\}/favorite/movies} y \texttt{GET /account/\{id\}/favorite/tv}, cada uno en bloques de 20 elementos. La función \texttt{fetchFavoritesForUser()} implementa un patrón de carga paralela: primero ejecuta una petición inicial para obtener el número total de páginas disponibles (\texttt{total\_pages}), y posteriormente lanza todas las páginas restantes mediante \texttt{Promise.all()}, procesándolas en lotes de 5 peticiones concurrentes para respetar los límites de la API de TMDb. Los resultados de películas y series se combinan en un único array marcando cada elemento con \texttt{media\_type: ``movie''} o \texttt{media\_type: ``tv''}.

El componente implementa además un sistema de caché para evitar peticiones redundantes. Las puntuaciones de OMDb (ratings de IMDb, Rotten Tomatoes y Metacritic) se almacenan en \texttt{sessionStorage} con clave \texttt{omdb:\{imdbId\}} y un TTL de 24 horas, permitiendo que los datos persistan durante la sesión de navegación actual sin necesidad de nuevas peticiones. Las puntuaciones de Trakt se guardan en \texttt{localStorage} con TTL de 24 horas y marcas de tiempo en formato epoch, garantizando persistencia entre sesiones. La carga de puntuaciones externas no se realiza hasta que el usuario activa el hover sobre una tarjeta, evitando decenas de peticiones durante el montaje inicial del componente.

La interfaz proporciona tres modos de visualización como botones en la barra de herramientas. El modo \textit{Grid} organiza los elementos en una cuadrícula responsive (6 columnas en escritorio) mostrando posters verticales en relación de aspecto 2:3, título y valoración de TMDb. El modo \textit{List} presenta filas horizontales con imagen backdrop en relación de aspecto 16:9 de resolución 1920$\times$1080 píxeles, título, año de estreno y puntuación TMDb y el modo \textit{Compact} muestra más elementos por fila (8 columnas en escritorio) y cada tarjeta con menor tamaño que en Grid.

El sistema permite alternar entre dos tipos de imagen de portada: \textit{Poster} (vertical, relación 2:3) o \textit{Backdrop} (horizontal, relación 16:9), controlado mediante el estado \texttt{imageMode}. La lógica implementada en \texttt{effectiveImageMode = viewMode === ``list'' ? ``backdrop'' : imageMode} fuerza el uso de backdrop cuando el modo de visualización es \textit{List}, independientemente de la preferencia del usuario, ya que este modo requiere imágenes horizontales para su diseño de filas. En los modos \textit{Grid} y \textit{Compact}, el usuario puede alternar libremente entre poster y backdrop mediante un botón específico en la barra de herramientas y la selección de imágenes prioriza contenido en inglés con mayor número de votos mediante \texttt{pickBestPosterEN()} para posters y \texttt{pickBestBackdropByLangResVotes()} para backdrops, con \textit{fallback} al \texttt{poster\_path} o \texttt{backdrop\_path} original de TMDb si la selección automática falla.

El menú de filtros y controles incluye cuatro categorías funcionales. La sección \textbf{Tipo} filtra el contenido mostrado mediante tres opciones: «Todo» (películas y series), «Películas» (\texttt{typeFilter = ``movies''}), o «Series» (\texttt{typeFilter = ``shows''}). La sección \textbf{Agrupar} organiza los elementos en grupos con cabeceras separadoras: «Sin agrupar» (predeterminado), «Por año» (usando \texttt{release\_date} o \texttt{first\_air\_date}), «Por década» (agrupando años en bloques de 10), «Género» (utilizando el primer género del array \texttt{genre\_ids}), y agrupación por valoración («TMDb», «IMDb», «Trakt», «Mis puntuaciones»). La sección \textbf{Orden} controla la ordenación de los elementos mediante ocho opciones: «Título A--Z» (\texttt{sortBy = ``title-asc''}), «Título Z--A» (\texttt{sortBy = ``title-desc''}), «Valoración ↓» y «Valoración ↑» (ordenación por \texttt{vote\_average} de TMDb), «Añadido reciente» y «Añadido antiguo» (ordenación por timestamp de inserción en favoritos de TMDb), y «Vista reciente» y «Vista antigua» (ordenación por fecha de último visionado obtenida de Trakt mediante \texttt{GET /sync/history}).

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{../notes/.images/favoritos.jpg}
    \caption{Interfaz de la sección de favoritos.}
    \label{fig:favoritos}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{../notes/.images/favoritos2.jpg}
    \caption{Vista tipo lista de la sección de favoritos.}
    \label{fig:favoritos-2}
\end{figure}

\subsection{Pendientes (\texttt{/watchlist})}

La página de watchlist muestra el contenido que el usuario tiene pendiente a partir de la API TMDb. El componente \texttt{WatchlistClient.jsx} utiliza la misma arquitectura que \texttt{FavoritesClient.jsx} y por ello cuenta con el mismo estilo de diseño y las mismas funcionalidades de filtrado, agrupación y ordenación. La única diferencia es que en lugar de consultar los endpoints de favoritos de TMDb, se consultan los endpoints de watchlist (\texttt{GET /account/\{id\}/watchlist/movies} y \texttt{GET /account/\{id\}/watchlist/tv}).

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{../notes/.images/pendientes.jpg}
    \caption{Interfaz de la sección de pendientes.}
    \label{fig:pendientes}
\end{figure}

\subsection{Historial de visionado (\texttt{/history})}

La página de historial muestra el registro completo de contenido visto por el usuario, obtenido desde Trakt mediante \texttt{GET /sync/history}. Trakt registra el timestamp exacto de cada visionado en formato ISO 8601, incluyendo la fecha, hora y episodio específico para series. Mediante la API Route interna \texttt{/api/trakt/history} se ajustan los datos de Trakt antes de enviarlos al cliente: traduce el identificador interno de Trakt a TMDb ID mediante \texttt{/search/tmdb/\{traktId\}?type=movie|show}, y ejecuta peticiones paralelas a TMDb para obtener \texttt{poster\_path}, \texttt{backdrop\_path} y sinopsis de cada título.

El componente \texttt{HistoryClient.jsx} organiza el historial mediante agrupación por día, mes y año. Cada grupo incluye cabeceras separadoras con la fecha correspondiente. Para series, la función \texttt{collapseConsecutive()} detecta episodios consecutivos de la misma serie vistos en la misma sesión y los agrupa en una entrada única. Cada tarjeta cuenta con un diseño hover como en la mayoría de secciones que muestra el número de episodio , fecha de visionado y título.

A parte del menú de herramientas que mantiene una estética unificada y permite aplicar filtros, ordenación y agrupación, existe un calendario interactivo que permite seleccionar cualquier fecha y también acceder a la vista completa por meses.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{../notes/.images/historial.jpg}
    \caption{Interfaz de la sección de historial de visionado.}
    \label{fig:historial-visionado}
\end{figure}

\subsection{En Progreso (\texttt{/in-progress})}

La página de series en progreso determina qué series se encuentran activas cuando al menos un episodio ha sido marcado como visto pero no todos los episodios disponibles han sido completados. \texttt{InProgressClient.jsx} combina la respuesta de \texttt{GET /sync/watched/shows} de Trakt, que contiene el número de episodios vistos por temporada, con los datos de TMDb que informan del número total de episodios emitidos hasta la fecha. Además, el componente cuenta con otra sección para las series completadas, que agrupa los títulos en los que todos los episodios disponibles han sido marcados como vistos mediante Trakt.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{../notes/.images/enprogreso.jpg}
  \caption{Interfaz de la sección de series en progreso.}
  \label{fig:en-progreso}
\end{figure}

\subsection{Estadísticas del usuario (\texttt{/stats})}

El módulo de estadísticas unifica los datos de visionado del usuario mediante una petición a la API Route interna \texttt{/api/trakt/user-stats}, que agrega \texttt{/users/me/stats} (resumen global del usuario), el historial completo de \texttt{/sync/history} y las listas de \texttt{watchedMovies} y \texttt{watchedShows} (para identificar títulos más vistos). La respuesta incluye el total de películas, series, episodios, temporadas, tiempo en red y distribución de géneros, además de listas de actores y directores más frecuentes en el historial.

\texttt{StatsClient.jsx} transforma estos datos en gráficas y secciones visuales e interactivas haciendo uso de Recharts. La sección principal presenta tarjetas KPI con el total de horas de visionado, películas vistas, series seguidas y episodios completados. El gráfico de actividad mensual es un \texttt{AreaChart} que muestra los últimos 12 meses. La distribución por día de la semana y por hora del día se representa mediante \texttt{BarChart}. La distribución de géneros se visualiza como un \texttt{RadarChart} que mapea los seis géneros más consumidos. El histograma de ratings (distribución de puntuaciones del usuario) adopta la forma de un \texttt{BarChart} horizontal..

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.88\textwidth,height=0.43\textheight,keepaspectratio]{../notes/.images/estadisticas1.jpg}

  \vspace{0.6em}

  \includegraphics[width=0.88\textwidth,height=0.43\textheight,keepaspectratio]{../notes/.images/estadisticas2.jpg}
  \caption{Interfaz de la sección de estadísticas del usuario (vista general y vista de patrones).}
  \label{fig:estadisticas-1-2}
\end{figure}

\subsection{Calendario de estrenos (\texttt{/calendar})}

El módulo de calendario obtiene los estrenos de películas desde TMDb mediante el endpoint de descubrimiento \texttt{/discover/movie}, parametrizado con rangos de fechas según el modo de visualización activo y la región España (\texttt{region=ES}). La construcción de la URL parametrizada incluye \texttt{release\_date.gte} y \texttt{release\_date.lte} para acotar el rango (día específico, semana o mes completo), junto con \texttt{sort\_by=release\_date.asc} para ordenar los resultados cronológicamente y \texttt{language=es-ES} para localización. En caso de rangos amplios (vista de mes), se implementa limitación de paginación (\texttt{page <= 5}) para evitar peticiones excesivas y mantener tiempos de respuesta razonables.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{../notes/.images/calendario.jpg}
  \caption{Interfaz del calendario de estrenos de películas.}
  \label{fig:calendario}
\end{figure}

\subsection{Biblioteca Personal (\texttt{/biblioteca})}

La biblioteca personal se basa en la integración con Plex Media Server. El componente \texttt{BibliotecaClient.jsx} consulta el endpoint interno \texttt{/api/plex} para obtener el inventario completo de la biblioteca del servidor local, incluyendo metadatos técnicos que solo Plex proporciona: resolución de cada archivo (4K/1080p/720p/576p/SD), códec de vídeo (H.264, HEVC/H.265, AV1), contenedor (\texttt{mkv}, \texttt{mp4}, \texttt{avi}), tamaño en disco en GB, y número de pistas de audio y subtítulos disponibles.

El componente implementa renderizado incremental para evitar bloquear el hilo principal cuando las bibliotecas Plex contienen un gran número de títulos, de esta forma renderiza los primeros 180 ítems de forma inmediata y añade bloques adicionales de 120 ítems cada vez que se alcanza el final de la lista, con prefetch de imágenes se muestran 80 ítems por delante de lo actualmente visible. Los datos de TMDb asociados a cada título (poster y backdrop en alta calidad) se cachean en un \texttt{Map}, y las preferencias de imagen se leen de \texttt{localStorage} con clave \texttt{showverse:movie:\{id\}:poster} antes de recurrir a la selección automática. La función \texttt{openPlexLink()} detecta la plataforma del usuario y genera la URL apropiada: \texttt{intent://} para Chrome en Android, el esquema \texttt{plex://} para la app nativa en iOS, o la URL web para escritorio.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{../notes/.images/biblioteca.jpg}
  \caption{Interfaz de la biblioteca personal integrada con Plex Media Server.}
  \label{fig:biblioteca}
\end{figure}

\subsection{Descubrir contenido (\texttt{/discover})}

La sección de descubrimiento emplea \texttt{/discover/movie} y \texttt{/discover/tv} de TMDb, que aceptan parámetros de filtrado completos, a diferencia de la búsqueda libre por texto que utiliza \texttt{/search/multi}. \texttt{DiscoverClient.jsx} gestiona el estado completo de los filtros activos: género, plataforma de \textit{streaming} disponible, certificación de edad, rango de valoración media y mínimo de votos, rango de fechas de estreno y palabras clave con autocompletado contra \texttt{/search/keyword} de TMDb. Todos estos filtros se combinan en una única petición estructurada a TMDb cuyos parámetros se calculan dinámicamente mediante \texttt{buildDiscoverParams()} a partir del estado activo.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{../notes/.images/descubrir.jpg}
  \caption{Interfaz de la sección Descubrir contenido.}
  \label{fig:descubrir}
\end{figure}

\subsection{Gestión de listas (\texttt{/lists})}

El módulo de listas unifica cuatro tipologías de contenido curatorado. Las listas propias del usuario en TMDb se obtienen mediante \texttt{GET /account/\{id\}/lists} y permiten operaciones completas de CRUD: creación de nuevas listas mediante formulario modal con nombre y descripción (petición \texttt{POST /list}), adición o eliminación de títulos desde la página de detalle, y edición de metadatos. Las colecciones de saga de TMDb (franquicias como el MCU, la saga de Bourne o el universo de Star Wars) se tratan como un tipo especial de lista que \texttt{CollectionDetailsClient.jsx} renderiza con un hero propio y un grid ordenado cronológicamente. Las listas públicas de Trakt se gestionan mediante \texttt{TraktListDetailsClient.jsx}, que implementa paginación con carga bajo demanda en páginas de 48 ítems mediante un botón «Cargar más» que consulta \texttt{/api/trakt/lists/\{username\}/\{listId\}?page=\{n\}\&limit=48}.

La página principal (\texttt{/lists}) precarga automáticamente los detalles de las primeras 3 listas del usuario (\texttt{PRELOAD\_FIRST\_N\_LISTS = 3}) para navegación instantánea al hacer clic. Las listas se muestran como tarjetas con un collage compuesto de hasta cuatro backdrops de los primeros títulos organizado en variantes de cuadrícula (1$\times$1 para listas con un título, 2$\times$1 para dos, 2$\times$2 para tres o más), generado por \texttt{ListCoverBackdropCollage()}. El diseño visual de cada lista incluye metadatos como el número de ítems, el nombre del creador y la descripción truncada, y permite ordenar las listas del usuario por fecha de creación, número de ítems o valoración media del contenido. \texttt{UnifiedListDetailsLayout.jsx} actúa como envoltorio de maquetación reutilizable para todas las páginas de detalle de lista, proporcionando el hero con título y metadatos, el sistema de pestañas con estado activo, y los controles superiores de filtrado y ordenación de forma consistente independientemente del tipo de lista (TMDb, colección o Trakt).

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{../notes/.images/listastrakt.jpg}
  \caption{Interfaz de las listas públicas de Trakt.}
  \label{fig:listas-trakt}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{../notes/.images/colecciones.jpg}
  \caption{Interfaz de la sección de colecciones.}
  \label{fig:colecciones}
\end{figure}

\subsection{Películas y Series (\texttt{/movies}, \texttt{/series})}

Las páginas de películas y series especializan el dashboard principal en un único tipo de contenido. \texttt{MoviesPageClient.jsx} y \texttt{SeriesPageClient.jsx} comparten la arquitectura técnica, incluyendo sistemas de caché a nivel de módulo para extras (\texttt{movieExtrasCache}), backdrops (\texttt{movieBackdropCache}), imágenes (\texttt{movieImagesCache}) y trailers (\texttt{movieTrailerCache} con mapa de peticiones en vuelo para deduplicación). \texttt{pickBestBackdropByLangResVotes()} prioriza backdrops de resolución 1920$\times$1080 sobre 2560$\times$1440 y 3840$\times$2160, priorizando la velocidad de carga sobre la calidad máxima de imagen. La selección de poster aplica \texttt{pickBestPosterByLangThenResolution()}, que calcula el área de cada imagen disponible (ancho $\times$ alto) y selecciona el de mayor resolución entre los disponibles en inglés o español, con \textit{fallback} al \texttt{poster\_path} original de TMDb.

La sección de trailers reproduce el tráiler oficial de cualquier título directamente en la página sin navegar al detalle. Cuando se hace hover sobre una tarjeta, \texttt{prefetchTrailer(id)} comprueba el caché de trailers en memoria y, si no hay datos cacheados, solicita los vídeos del título a TMDb mediante \texttt{GET /\{type\}/\{id\}/videos} y almacena el resultado. Al hacer clic en el botón de reproducción, el componente abre \texttt{VideoModal} con el \texttt{videoKey} de YouTube del tráiler. Ambos módulos incluyen integración directa con los botones de favorito y watchlist de TMDb desde las tarjetas del grid, usando el patrón de actualización optimista descrito en el módulo de detalles, sin necesidad de navegar a la página de detalle para gestionar las listas personales.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{../notes/.images/peliculas.jpg}
  \caption{Interfaz de la sección de películas.}
  \label{fig:peliculas}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{../notes/.images/series.jpg}
  \caption{Interfaz de la sección de series.}
  \label{fig:series}
\end{figure}

\subsection{Ficha de personas (\texttt{/details/person/[id]})}

La ficha de persona implementada en \texttt{ActorDetails.jsx} carga en paralelo mediante \texttt{Promise.allSettled()} cinco recursos independientes: identificadores externos (\texttt{/person/\{id\}/external\_ids}), créditos combinados de películas y series (\texttt{/person/\{id\}/combined\_credits?language=es-ES}), galería de fotos de perfil (\texttt{/person/\{id\}/images}), imágenes promocionales etiquetadas (\texttt{/person/\{id\}/tagged\_images}) y traducciones disponibles (\texttt{/person/\{id\}/translations}), escogiendo la versión en español de la biografía si está disponible. El uso de \texttt{Promise.allSettled()} en lugar de \texttt{Promise.all()} garantiza que un fallo en uno de los recursos (por ejemplo, ausencia de imágenes etiquetadas) no cancele la carga de los demás.

La filmografía completa se computa como un valor derivado mediante \texttt{useMemo()} a partir del array \texttt{combined\_credits}, normalizando el formato heterogéneo de los campos según si el ítem es una película (\texttt{release\_date}) o una serie (\texttt{first\_air\_date}), y si el crédito es como actor (\texttt{character}) o como miembro del equipo técnico (\texttt{job}, \texttt{department}). Los filtros disponibles son: tipo de medio (todas, películas, series), tipo de crédito (actuación, equipo técnico), departamento (para filtrar directores, guionistas, productores dentro del equipo técnico), rango de años desde/hasta, y búsqueda libre por título o nombre de personaje. Todos los filtros se aplican de forma combinada en un único paso de procesamiento sobre el array completo, sin peticiones adicionales al servidor. La ordenación por popularidad, fecha, valoración o alfabético se aplica como etapa posterior al filtrado. El sistema calcula estadísticas sobre la filmografía filtrada (total de créditos, películas, series, actuaciones, roles técnicos) que se muestran como tarjetas informativas en la cabecera, actualizándose en tiempo real al cambiar los filtros. La función \texttt{calcAge()} calcula la edad actual o la edad en el momento del fallecimiento a partir de las fechas de nacimiento y muerte, y \texttt{formatHumanDate()} formatea las fechas en español con localización \texttt{es-ES}.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{../notes/.images/persona.jpg}
  \caption{Interfaz de la ficha de persona.}
  \label{fig:persona}
\end{figure}

\subsection{Barra de navegación (Navbar)}

La barra de navegación implementada en \texttt{Navbar.jsx} se encuentra presente en todas las páginas de la aplicación, ya que se instancia directamente en el \texttt{layout.jsx} raíz. En escritorio adopta una disposición horizontal en tres zonas: el logotipo y nombre de la aplicación a la izquierda, una barra de búsqueda centralizada, y a la derecha un conjunto de iconos de navegación con códigos de color por categoría temática (estadísticas en índigo, listas en morado, calendario en ámbar, series en progreso en verde, historial en rojo, favoritos en rojo, watchlist en azul). En móvil, la barra superior simplifica el contenido a logo, buscador y avatar, mientras que la navegación principal se traslada a una barra fija de iconos en la parte inferior de la pantalla accesible con el pulgar, complementada por un menú deslizable lateral (\textit{drawer}) que contiene los enlaces secundarios.

La barra de búsqueda \texttt{SearchBar} se reutiliza tanto en la versión de escritorio como en la modal de búsqueda de móvil. Implementa un debounce de 300\,ms antes de lanzar cada consulta a \texttt{/search/multi?query=...\&language=es-ES}, y cancela las peticiones anteriores con \texttt{AbortController} para evitar que resultados de búsquedas superadas sobrescriban los de la búsqueda actual. El \textit{dropdown} de resultados muestra hasta diez sugerencias con el tipo de contenido codificado por color (badge azul para películas, badge verde para series, badge naranja para personas), la foto de perfil o poster en miniatura, el año de estreno y el rating de TMDb. Una variante del componente Navbar incluye el botón \texttt{TraktHistoryNavButton} que muestra en tiempo real cuántos ítems hay en la lista de pendientes, obtenido del contexto de autenticación \texttt{AuthContext} sin petición adicional. El efecto de transición de transparente a \textit{glassmorphism} al hacer \textit{scroll} se implementa con un listener \texttt{scroll} que aplica o elimina la clase \texttt{bg-black/70 backdrop-blur-12px} al superar un umbral de 100\,px.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{../notes/.images/navbar-ordenador.jpg}
  \caption{Barra de navegación en escritorio.}
  \label{fig:navbar-ordenador}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.25\textwidth]{../notes/.images/navbar-movil.jpg}
  \caption{Barra de navegación en móvil.}
  \label{fig:navbar-movil}
\end{figure}

% -----------------------------------------------------------------------
\subsection{Patrones de optimización transversales}

La aplicación implementa un conjunto de patrones de optimización aplicados de forma sistemática que explican los tiempos de respuesta percibidos como inmediatos a pesar de integrar datos de seis APIs externas distintas.

\textbf{Actualización optimista.} Todas las acciones de escritura del usuario (marcar como favorito, añadir a pendientes, valorar) actualizan la interfaz instantáneamente sin esperar la confirmación del servidor. La petición real se envía en paralelo en segundo plano, y solo si falla se revierte el cambio visual mostrando un mensaje de error. Este patrón elimina la latencia perceptible de 300 a 600 milisegundos que tendría una actualización sincrónica, creando la sensación de respuesta inmediata característica de las aplicaciones nativas.

\textbf{Precarga de imágenes.} Las imágenes de posters y backdrops se precargan con un margen adelantado respecto al viewport, de forma que cuando el usuario hace scroll las imágenes ya están disponibles en la caché del navegador, evitando tiempos de carga visibles.

\textbf{Control de concurrencia.} Para respetar los límites de las APIs externas sin renunciar al paralelismo, se mantiene un máximo de 4 o 5 peticiones concurrentes. Esto reduce el tiempo de carga de colecciones paginadas de N peticiones secuenciales a aproximadamente N/4 rondas paralelas, mejorando el rendimiento entre 4 y 5 veces respecto a la ejecución secuencial.

\textbf{Cancelación de peticiones obsoletas.} Cuando el usuario navega a otra página mientras hay peticiones en curso, estas se cancelan automáticamente para prevenir condiciones de carrera y actualizaciones de estado en componentes ya desmontados.

\textbf{Regeneración estática incremental (ISR).} Los datos semi-estables como catálogos de tendencias y listas de mejor valoradas se sirven con regeneración cada 10 a 30 minutos, permitiendo que la mayoría de visitas utilicen páginas preconstruidas desde la CDN sin ejecutar peticiones a APIs externas.

% -----------------------------------------------------------------------
\section{Diseño de la interfaz}
% -----------------------------------------------------------------------

\subsection{Sistema de diseño global}

El sistema de diseño parte de un principio rector: el contenido visual proveniente de TMDb (backdrops y posters de cada título) es el protagonista de la interfaz. La paleta de colores, los efectos de superficie y las estrategias de iluminación se han diseñado para potenciar, no competir con, estas imágenes.

\textbf{Paleta de colores.} El color de fondo global es negro puro, sobre el cual se despliegan los backdrops a pantalla completa con gradientes perimetrales que los integran suavemente. Este enfoque traslada la estética de las plataformas de streaming (modo teatro) a la interfaz de seguimiento.

\textbf{Superficies de cristal (glassmorphism).} Las tarjetas, paneles laterales, la barra de navegación y los menús desplegables utilizan un efecto de cristal semitransparente: fondo blanco con 5\% de opacidad, desenfoque de 10 píxeles del contenido subyacente y borde sutil de 1 píxel. La opacidad baja mantiene la legibilidad del texto mientras proyecta el color del backdrop en la superficie del elemento. El desenfoque se procesa mediante GPU y no genera sobrecarga en el renderizado.

\textbf{Tipografía.} El proyecto utiliza la familia Geist Sans y Geist Mono, tipografías de Vercel distribuidas como fuentes variables. Se cargan como fuentes del sistema sin peticiones externas, evitando el problema de \textit{Flash of Unstyled Text} (FOUT) durante la carga inicial.

\textbf{Accesibilidad y movimiento reducido.} El sistema respeta las preferencias de accesibilidad del sistema operativo, desactivando automáticamente todas las transiciones y animaciones CSS para usuarios que han configurado la opción de movimiento reducido en su dispositivo.

\subsection{Componentes de Next.js utilizados}

\textbf{\texttt{<Image>} de Next.js.} Todos los pósters, \textit{backdrops} y logos de la aplicación se renderizan mediante el componente \texttt{<Image>} de Next.js en lugar del elemento \texttt{<img>} nativo. Este componente aplica automáticamente:

\begin{itemize}
\item Conversión del formato original JPEG a WebP para los navegadores que lo soportan, reduciendo el peso de las imágenes entre un 25 y un 35\% sin pérdida visual apreciable.
\item \textit{Lazy loading} nativo: las imágenes fuera del \textit{viewport} no se solicitan hasta que el usuario se aproxima a ellas.
\item Reserva de espacio (\textit{placeholder}) mediante \texttt{width} y \texttt{height} explícitos, evitando el salto de layout (CLS) que se produciría si la imagen tardase en cargar.
\item Optimización automática a través del CDN de Vercel, que sirve cada imagen en el tamaño exacto requerido por el dispositivo del usuario.
\end{itemize}

\textbf{\texttt{<Link>} de Next.js.} La navegación entre páginas utiliza el componente \texttt{<Link>} del App Router de Next.js, que implementa \textit{prefetching} automático de las rutas visibles en el \textit{viewport}. En producción (Vercel), cuando el usuario se aproxima a un enlace, Next.js precarga en segundo plano los datos del Server Component de la ruta destino, logrando transiciones de página percibidas como instantáneas.

\textbf{\texttt{usePathname} y estado activo en la Navbar.} El componente \texttt{Navbar.jsx} utiliza el \textit{hook} \texttt{usePathname()} de \texttt{next/navigation} para detectar la ruta activa y aplicar el estilo visual correspondiente a cada enlace de navegación sin necesidad de estado global. La función \texttt{isActive(href)} comprueba si la ruta actual comienza con el \textit{href} del enlace, marcando toda la sección como activa aunque el usuario esté en una subruta.

\subsection{Componentes propios: Navbar}

El componente \texttt{Navbar.jsx} (~30\,KB) implementa una navegación adaptativa con dos modos de presentación.

\textbf{Modo escritorio (lg+).} Una barra horizontal fija en la parte superior con tres zonas:
\begin{itemize}
\item \textbf{Izquierda}: logotipo de la aplicación y navegación principal (Inicio, Películas, Series, Descubrir, Biblioteca).
\item \textbf{Centro} (posición absoluta): campo de búsqueda en tiempo real con \textit{dropdown} de resultados.
\item \textbf{Derecha}: botones de acceso rápido a Estadísticas, Listas, Calendario, En Progreso, Historial, Favoritas y Pendientes; avatar del usuario autenticado.
\end{itemize}

Cada botón de icono utiliza un sistema de estilos por «tono de color» (\texttt{iconLinkClass(href, tone)}): al hacer \textit{hover}, el botón adquiere el color semántico de su función (rojo para Favoritas, azul para Pendientes, ámbar para Calendario, verde para En Progreso, etc.) junto con un halo de sombra coloreado:
\begin{verbatim}
hover:shadow-[0_0_18px_rgba(239,68,68,0.16)]
\end{verbatim}

Cuando el estado está activo (ruta actual), el color se intensifica con un fondo semitransparente y una sombra más pronunciada.

\textbf{Modo móvil.} La barra superior se simplifica al logotipo centrado, un botón de menú (hamburgesa) y un botón de búsqueda. En la parte inferior de la pantalla aparece una barra de navegación fija (\textit{tab bar}) con las cuatro secciones de mayor uso. Esta arquitectura de doble barra es el patrón estándar de las aplicaciones de contenido multimedia en iOS y Android.

El \textbf{drawer lateral} del menú móvil se implementa con Framer Motion:
\begin{verbatim}
<motion.aside
  initial={{ x: -320 }}
  animate={{ x: 0 }}
  exit={{ x: -320 }}
  transition={{ type: "tween", duration: 0.22 }}
>
\end{verbatim}

El fondo oscuro detrás del drawer (\texttt{bg-black/70 backdrop-blur-sm}) también se anima con \texttt{AnimatePresence} para que tanto la entrada como la salida del menú sean fluidas.

La \textbf{búsqueda en móvil} abre un \textit{overlay} a pantalla completa (\texttt{bg-black/95 backdrop-blur-2xl}) que entra con una animación de escala y opacidad (\texttt{scale: 0.95$\rightarrow$1, opacity: 0$\rightarrow$1}), dando la sensación de que emerge desde el botón de búsqueda.

\subsection{Componente propio: LiquidButton}

\texttt{LiquidButton.jsx} (~18\,KB) es el componente de botón de acción rápida utilizado en la página de detalle para las operaciones de favoritos, pendientes, historial y valoración personal. Es un Client Component completamente autónomo que implementa un sistema de efectos visuales interactivos mediante la API \texttt{Canvas 2D} del navegador.

La arquitectura del componente se basa en tres capas superpuestas dentro de un \texttt{<button>}:
\begin{enumerate}
\item \textbf{Canvas de efectos}: un elemento \texttt{<canvas>} que ocupa toda la superficie del botón y gestiona, mediante \texttt{requestAnimationFrame}, las partículas flotantes, las ondulaciones de \textit{ripple} y los efectos de explosión al hacer clic.
\item \textbf{Superposición de brillo}: un \texttt{<div>} con un gradiente lineal animado mediante CSS (\texttt{@keyframes liquidShine}) que recorre la superficie del botón de forma continua cuando está activo o en \textit{hover}.
\item \textbf{Borde pulsante}: un segundo \texttt{<div>} circular con borde coloreado animado mediante \texttt{@keyframes liquidPulse} (escala 1 $\rightarrow$ 1.15 $\rightarrow$ 1, con periodo de 2 s).
\end{enumerate}

El sistema de efectos de \textit{canvas} implementa:
\begin{itemize}
\item \textbf{Partículas flotantes}: 12 partículas con movimiento senoidal suave que se generan al entrar en \textit{hover}. Cada partícula tiene posición, velocidad, tamaño, opacidad y fase trigonométrica independientes.
\item \textbf{Ripple (ondulación)}: al hacer \textit{hover} o clic, se crean ondas circulares que se expanden desde el punto de contacto y se desvanecen al alcanzar su radio máximo.
\item \textbf{Trail effect}: al mover el ratón sobre el botón, se generan puntos de rastro que se desvanecen en 0,4 s, creando un efecto de «estela líquida».
\item \textbf{Explosión}: al hacer clic, se generan 12 partículas que se dispersan en ángulos uniformes desde el punto de impacto con gravedad simulada (\texttt{vy += 0.12}) y decaimiento de opacidad.
\end{itemize}

El componente soporta un sistema de \textbf{propagación entre botones del mismo grupo} (\texttt{groupId}): al interactuar con un botón, este emite un \texttt{CustomEvent} al documento (\texttt{liquid-spread}, \texttt{liquid-click}, \texttt{liquid-proximity}) que es capturado por todos los botones del mismo grupo. Los botones próximos a la fuente del evento reaccionan con ondulaciones de intensidad proporcional a la distancia, creando la sensación de que el líquido se propaga físicamente entre botones adyacentes.

El color activo del botón es configurable mediante la prop \texttt{activeColor} (blue, red, yellow, purple, green, teal, orange), lo que permite usar el mismo componente para acciones semánticamente distintas manteniendo coherencia visual.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.6\textwidth]{../notes/.images/botonesaccion.jpg}
  \caption{Botones de acción implementados con el componente LiquidButton.}
  \label{fig:botones-accion}
\end{figure}

\subsection{Componente propio: StarRating}

\texttt{StarRating.jsx} gestiona la valoración personal del usuario en Trakt (escala 1--10). Utiliza \texttt{LiquidButton} como \textit{trigger} con \texttt{activeColor=''yellow''}: si el usuario ya tiene una valoración, el botón muestra el valor numérico con el efecto de partículas activo; si no, muestra el icono de estrella.

Al hacer clic, el componente abre un modal renderizado mediante \texttt{createPortal()} directamente en \texttt{document.body}, evitando problemas de apilamiento CSS (\texttt{z-index}) con los elementos de la página de detalle. El modal contiene:
\begin{itemize}
\item Un número grande que muestra el valor actual centrado sobre una estrella decorativa semitransparente.
\item Un \textit{slider} HTML nativo (\texttt{<input type=``range''>}) con paso de 0,5 y recorrido de 1 a 10, cuyo \textit{thumb} nativo se oculta con \texttt{opacity: 0} y se sustituye por un \textit{thumb} personalizado posicionado con CSS (\texttt{left: calc(...)}).
\item Botones de ajuste fino $-0{,}5$ / $+0{,}5$ a ambos lados del \textit{slider}.
\item Botón de confirmación y, si ya existe una valoración, botón de borrado con icono de papelera.
\end{itemize}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.5\textwidth]{../notes/.images/puntuacion.jpg}
  \caption{Interfaz del componente de valoración personal StarRating.}
  \label{fig:puntuacion}
\end{figure}

\subsection{Componente propio: DetailsSectionMenu}

\texttt{DetailsSectionMenu.jsx} gestiona la navegación interna de la página de detalle mediante pestañas contextuales. Su función principal es organizar el contenido por bloques funcionales (Media, Comentarios, Listas, Reparto, Recomendaciones, Temporadas y Episodios) y adaptar automáticamente qué pestañas se muestran según el tipo de contenido (película o serie) y según la disponibilidad real de datos.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{../notes/.images/detailssectionmenu.jpg}
  \caption{Interfaz del componente DetailsSectionMenu.}
  \label{fig:detailssectionmenu}
\end{figure}

\subsection{Componente propio: EpisodeRatingsGrid}

\texttt{EpisodeRatingsGrid.jsx} presenta una vista matricial de episodios para series, donde cada celda representa un episodio y su color refleja la puntuación. Este componente permite detectar de forma rápida patrones de calidad por temporada y ofrece navegación directa al detalle de episodio cuando el usuario hace clic en una celda.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.5\textwidth]{../notes/.images/gridratings.jpg}
  \caption{Interfaz del componente EpisodeRatingsGrid.}
  \label{fig:gridratings}
\end{figure}

\subsection{Componente propio: ScoreboardBar}

\texttt{ScoreboardBar.jsx} centraliza en una sola franja la información de valoración y contexto del título, para evitar que el usuario tenga que consultar varias secciones distintas de la ficha. En este componente se agrupan:

\begin{itemize}
\item Puntuaciones de TMDb, Trakt e IMDb.
\item Número de votos asociado a las fuentes disponibles.
\item Estadísticas de Trakt vinculadas al seguimiento del título.
\item Botón de compartir para difundir directamente el contenido.
\end{itemize}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.7\textwidth]{../notes/.images/scoreboard.jpg}
  \caption{Interfaz del componente ScoreboardBar.}
  \label{fig:scoreboard}
\end{figure}

\subsection{Componentes propios: control de visionado en Trakt}

El bloque de control de visionado se apoya en tres componentes coordinados:
\begin{itemize}
\item \texttt{TraktEpisodesWatchedModal}: modal para gestionar el marcado de episodios en series de forma más detallada.
\item \texttt{TraktWatchedModal}: modal de historial de visionados del título en Trakt.
\end{itemize}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.7\textwidth]{../notes/.images/episodiosvistos.jpg}
  \caption{Interfaz del modal TraktEpisodesWatchedModal para gestionar episodios vistos.}
  \label{fig:episodiosvistos}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.5\textwidth]{../notes/.images/historialvisionado.jpg}
  \caption{Interfaz del modal TraktWatchedModal con el historial de visionado.}
  \label{fig:historialvisionado}
\end{figure}

En conjunto, estos componentes centralizan las acciones de seguimiento y sincronización de visionado sin sacar al usuario de la ficha.

\subsection{Componentes propios: modales de apoyo en DetailsClient}

\texttt{DetailsClient.jsx} utiliza varios modales especializados para acciones concretas:
\begin{itemize}
\item \texttt{AddToListModal}: añadir o quitar el título de listas personalizadas de TMDb.
\item \texttt{VideoModal}: reproducir trailers y vídeos oficiales dentro de la propia página.
\item \texttt{ExternalLinksModal}: agrupar enlaces externos en móvil cuando no hay espacio para mostrar todos los iconos.
\end{itemize}

Como apoyo adicional en la misma pantalla también es relevante \texttt{SeasonDetailsClient} (detalle expandible de temporadas y episodios).

\subsection{Sistema de animaciones con Framer Motion}

Framer Motion gestiona las animaciones de mayor complejidad del proyecto a través de cuatro patrones principales:

\textbf{1. Animaciones de entrada escalonada (\texttt{staggerChildren}).} En el dashboard y en las páginas de listas, las tarjetas no aparecen todas simultáneamente. La variante del contenedor define un \texttt{staggerChildren} de 0,05\,s (reducido a 0,02\,s para listas largas de más de 30 ítems), de forma que cada tarjeta entra con un ligero retraso respecto a la anterior:
\begin{verbatim}
const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: { staggerChildren: 0.05 }
  }
};
const itemVariants = {
  hidden: { opacity: 0, y: 20 },
  visible: { opacity: 1, y: 0 }
};
\end{verbatim}

\textbf{2. \texttt{AnimatePresence} para salidas animadas.} El sistema de tres vistas (Grid, List, Compact) y las pestañas de la página de detalle utilizan \texttt{AnimatePresence} para animar tanto la entrada como la salida de los elementos del DOM. Cuando el usuario cambia de vista, el contenido saliente realiza una animación de salida (\texttt{exit}) antes de ser eliminado del DOM, evitando cortes bruscos:
\begin{verbatim}
<AnimatePresence mode="wait">
  <motion.div
    key={currentView}
    initial={{ opacity: 0, x: 20 }}
    animate={{ opacity: 1, x: 0 }}
    exit={{ opacity: 0, x: -20 }}
    transition={{ duration: 0.2 }}
  />
</AnimatePresence>
\end{verbatim}

El modo \texttt{''wait''} garantiza que la animación de salida se complete antes de que empiece la de entrada.

\textbf{3. Drawer y overlays móviles.} El menú lateral, la búsqueda en \textit{overlay} móvil y los paneles modales utilizan \texttt{motion.div} con valores iniciales fuera del \textit{viewport}: \texttt{x: -320} para el drawer (deslizamiento desde la izquierda), \texttt{scale: 0.95, opacity: 0} para los modales (apertura con escala). Las transiciones de tipo \texttt{tween} con \texttt{ease: ''easeOut''} producen una deceleración natural al final del movimiento.

\textbf{4. Carrusel del hero.} El carrusel automático de la página principal anima las transiciones entre slides con una combinación de \texttt{opacity} (entrada: 0$\rightarrow$1, salida: 1$\rightarrow$0) y un ligero desplazamiento horizontal (\texttt{x: +20$\rightarrow$0}). La pausa automática al hacer \textit{hover} se gestiona mediante un \texttt{useEffect} que detecta el evento \texttt{mouseenter} del contenedor del hero.

\textbf{Performance de las animaciones.} Todas las animaciones de Framer Motion utilizan exclusivamente las propiedades CSS \texttt{transform} (para \texttt{x}, \texttt{y}, \texttt{scale}, \texttt{rotate}) y \texttt{opacity}, que el navegador puede procesar enteramente en la GPU sin necesidad de recalcular el \textit{layout} ni repintar capas del DOM. Esto garantiza que las animaciones se ejecuten a 60 fps incluso en dispositivos de gama media.

\subsection{Sistema de estilos global (\texttt{globals.css})}

El fichero \texttt{globals.css} define un conjunto de utilidades y animaciones CSS que complementan a Tailwind CSS:

\begin{itemize}
\item \textbf{\texttt{.no-scrollbar}}: oculta la barra de desplazamiento en los carruseles horizontales y los paneles de pestañas, sin deshabilitar el \textit{scroll} táctil.
\item \textbf{\texttt{.sv-scroll}}: barra de desplazamiento personalizada para los paneles con mucho contenido (lista de episodios, resultados de búsqueda): fina, semitransparente, con bordes redondeados y espacio interior.
\item \textbf{\texttt{.animate-shimmer}}: efecto de carga tipo \textit{skeleton} con un gradiente que avanza horizontalmente (\texttt{background-position: -200\% $\rightarrow$ 200\%}) sobre los \textit{placeholders} de las tarjetas mientras los datos se cargan.
\item \textbf{\texttt{.hover-lift}}: eleva el elemento 4\,px y añade una sombra al hacer \textit{hover}, indicando que es interactivo.
\item \textbf{\texttt{.glass} / \texttt{.glass-dark}}: superficies de \textit{glassmorphism} en variante clara y oscura reutilizables en cualquier componente.
\end{itemize}

\subsection{Sistema de tres vistas (Grid, List, Compact)}

El sistema de tres vistas es un componente transversal utilizado en Favoritos, Pendientes, Historial y En Progreso. La selección de vista se persiste en \texttt{localStorage} bajo una clave específica por módulo (\texttt{showverse:view:favorites}, \texttt{showverse:view:history}, etc.), de forma que el usuario puede tener configuraciones distintas en cada sección.

La transición entre vistas utiliza \texttt{AnimatePresence} de Framer Motion con \texttt{mode=''wait''} y un efecto de \textit{cross-fade} suave: el contenido saliente desaparece con \texttt{opacity: 1$\rightarrow$0} antes de que el nuevo contenido entre con \texttt{opacity: 0$\rightarrow$1}. Esto evita los saltos bruscos de \textit{layout} que se producirían si los dos estados coexistieran en el DOM simultáneamente.

% -----------------------------------------------------------------------
\section{Rendimiento en producción}
% -----------------------------------------------------------------------

Las métricas obtenidas en producción (Vercel) mediante Lighthouse validan las decisiones de arquitectura tomadas:

\begin{table}[!htbp]
\begin{center}
\begin{tabular}{| l | c | c |}
\hline
\textbf{Métrica} & \textbf{Valor obtenido} & \textbf{Objetivo} \\ 
\hline
\hline
Performance (Lighthouse) & 92/100 & >90 \\
\hline
SEO (Lighthouse) & 100/100 & 100 \\
\hline
Best Practices & 95/100 & >90 \\
\hline
LCP & 1,8 s & <2,5 s \\
\hline
CLS & 0,05 & <0,1 \\
\hline
FID & 45 ms & <100 ms \\
\hline
\end{tabular}
\caption{Métricas Lighthouse en producción}
\label{tab:lighthouse}
\end{center}
\end{table}

El LCP de 1,8\,s se consigue principalmente gracias al SSR: el HTML con el \textit{hero} ya renderizado llega al navegador antes de que se descargue todo el JavaScript. El CLS de 0,05 se logra mediante el uso sistemático de \texttt{width} y \texttt{height} explícitos en el componente \texttt{<Image>} de Next.js \parencite{Google:2024a}.

% -----------------------------------------------------------------------
\section{Sistema de caché multinivel}
% -----------------------------------------------------------------------

El rendimiento de \textit{The Show Verse} depende críticamente de la estrategia de caché implementada, que opera en cuatro niveles diferenciados según el tipo de dato y su tasa de cambio.

\subsection{Nivel 1: Incremental Static Regeneration (ISR)}

Las páginas que consumen datos de TMDb en el servidor utilizan ISR mediante la opción \texttt{next: \{revalidate: TTL\}} del sistema de caché de Next.js. Por defecto, el módulo \texttt{lib/api/tmdb.js} aplica una revalidación de 10 minutos para todas las peticiones realizadas en Server Components, equilibrando frescura de datos con carga del servidor.

Este valor puede sobrescribirse pasando opciones personalizadas en casos específicos. Para datos personales que requieren autenticación (favoritos, historial, watchlist), las peticiones se configuran con \texttt{cache: no-store} para evitar que se sirvan datos obsoletos de otros usuarios.

\subsection{Nivel 2: Caché en memoria del proceso Node.js}

La API Route \texttt{/api/omdb} implementa un sistema de caché persistente durante toda la vida del proceso serverless de Node.js mediante estructuras \texttt{Map} almacenadas en \texttt{globalThis}. Esta caché distingue entre respuestas exitosas y fallidas con estrategias de TTL diferenciadas.

Las respuestas exitosas se almacenan durante 24 horas, permitiendo que múltiples peticiones HTTP reutilicen el mismo dato sin consultar la API externa de OMDb. Las respuestas fallidas (errores 404, timeouts de red) se cachean solamente durante 60 segundos, lo que permite reintentos rápidos sin sobrecargar la API externa si el problema es temporal.

Al ejecutarse en Vercel, esta caché persiste mientras la instancia serverless se mantenga activa. Las instancias frías pierden la caché y deben reconstruirla, pero el sistema garantiza que esto no afecte a la experiencia del usuario gracias al nivel siguiente de deduplicación.

\subsection{Nivel 3: Deduplicación de peticiones concurrentes}

El sistema implementa un mecanismo de deduplicación mediante un segundo \texttt{Map} global que registra las promesas de peticiones HTTP en curso. Cuando múltiples componentes solicitan simultáneamente el mismo \texttt{imdbId} (por ejemplo, 10 tarjetas cargándose en paralelo), solo se ejecuta una petición real a OMDb.

El resultado de esta petición única se comparte con todos los solicitantes concurrentes. Una vez completada la petición, la promesa se elimina del mapa de deduplicación y el resultado se almacena en la caché persistente del Nivel 2. Esta estrategia reduce el tráfico hasta 10 veces durante la carga inicial de listas extensas.

\subsection{Nivel 4: Caché en el navegador (sessionStorage)}

Los componentes del cliente que muestran listas de contenido implementan caché de puntuaciones en el almacenamiento de sesión del navegador. Los datos de OMDb (ratings de IMDb, Rotten Tomatoes y Metacritic) se almacenan bajo la clave \texttt{showverse:omdb:\{imdbId\}} con un tiempo de vida de 24 horas.

Esta caché persiste durante toda la navegación del usuario, incluyendo recargas de página, pero se elimina automáticamente al cerrar la pestaña del navegador. El sistema valida la frescura de los datos comparando la marca de tiempo almacenada con el tiempo actual antes de utilizarlos, descartando automáticamente los datos expirados.

% -----------------------------------------------------------------------
\section{Middleware y optimización SEO}
% -----------------------------------------------------------------------

El sistema implementa una estrategia de optimización para asegurar que los bots de redes sociales y motores de búsqueda puedan indexar correctamente el contenido de la aplicación. Esto es fundamental porque, al ser una aplicación React basada en componentes del lado del cliente, los bots no ejecutarían el JavaScript y recibirían un HTML vacío sin metadatos útiles.

\subsection{Detección de bots y reescritura de rutas}

El middleware intercepta las peticiones entrantes e identifica si provienen de bots (WhatsApp, Facebook, Twitter, Telegram, Discord, LinkedIn, Googlebot, Bingbot, etc.) mediante el análisis del encabezado \texttt{User-Agent}. Cuando se detecta un bot accediendo a una página de detalle (como \texttt{/details/movie/550}), el sistema reescribe transparentemente la URL a una ruta alternativa (\texttt{/s/movie/550}) que sirve contenido HTML estático con todos los metadatos necesarios.

Esta reescritura es completamente transparente: la URL visible no cambia, pero internamente el bot recibe una respuesta HTML completa con toda la información de Open Graph y Twitter Cards. El patrón limita su ejecución exclusivamente a las rutas de detalle, evitando cualquier sobrecarga en el resto de la aplicación.

\subsection{Generación de HTML optimizado para compartir}

Las rutas alternativas (\texttt{/s/*}) construyen páginas HTML estáticas que incluyen:

\begin{itemize}
\item \textbf{Metadatos Open Graph}: título, descripción, tipo de contenido (película, serie o persona), URL canónica e imagen representativa.
\item \textbf{Twitter Cards}: información adaptada para previsualizaciones en Twitter/X.
\item \textbf{Selección inteligente de imágenes}: se prioriza el uso de backdrops horizontales (1280$\times$720 píxeles) sobre posters verticales, ya que las redes sociales muestran previsualizaciones en formato apaisado que aprovechan mejor este tipo de imágenes.
\item \textbf{Redirección automática para usuarios reales}: si un humano accede directamente a estas rutas, se le redirige inmediatamente a la página interactiva completa mediante una metaetiqueta \texttt{refresh}.
\end{itemize}

El sistema aplica una estrategia de caché de dos niveles: la CDN almacena estas respuestas durante 24 horas y puede servir versiones ligeramente obsoletas hasta 7 días más mientras regenera el contenido en segundo plano. Esto permite que el 99\% de las peticiones de bots se resuelvan desde la CDN sin ejecutar código en el servidor, reduciendo drásticamente la carga y mejorando los tiempos de respuesta.

\subsection{Metadatos globales y por página}

El archivo \texttt{layout.jsx} raíz define los metadatos por defecto de la aplicación mediante el objeto estático \texttt{metadata}:

\begin{verbatim}
export const metadata = {
  title: 'The Show Verse',
  description: 'Tu plataforma de películas y series',
  manifest: '/site.webmanifest',
  icons: {
    icon: [
      { url: '/icon.png', sizes: '256x256', type: 'image/png' },
      { url: '/icon-512.png', sizes: '512x512' },
    ]
  }
}
\end{verbatim}

\subsection{Resumen de la arquitectura dual de rutas}

El flujo completo de una petición a \texttt{/details/movie/550} funciona de la siguiente manera:

\begin{enumerate}
\item La petición llega al \textit{middleware} Edge de Next.js.
\item El middleware evalúa el \texttt{User-Agent} contra la expresión regular \texttt{BOT\_UA}.
\item \textbf{Si es un bot:} reescritura transparente a \texttt{/s/movie/550}. El Route Handler devuelve HTML con Open Graph completo, cacheado 24h en CDN.
\item \textbf{Si es un usuario:} la petición pasa sin modificar a \texttt{/details/movie/550}. Se ejecuta el Server Component que hidrata el Client Component interactivo.
\end{enumerate}

Esta arquitectura garantiza:
\begin{itemize}
\item Los bots indexan las rutas \texttt{/s/*} con metadatos completos (OGP, Twitter Cards, canonical).
\item Los usuarios navegan las rutas \texttt{/details/*} con el Client Component completo e interactivo.
\item No hay contenido duplicado: un usuario que acceda directamente a \texttt{/s/movie/550} es redirigido por el \texttt{<meta refresh>} a la ruta interactiva.
\item El 99\% del tráfico de bots se resuelve desde la CDN sin invocar funciones serverless.
\end{itemize}

% -----------------------------------------------------------------------
\section{Sistema de artwork personalizado}
% -----------------------------------------------------------------------

El sistema de \textit{artwork} personalizado permite sustituir manualmente el \textit{poster}, \textit{backdrop} o imagen de fondo asociados a una película o serie. Para ello se utiliza un archivo de configuración en la raíz del proyecto, desde el que se definen sobrescrituras vinculadas al identificador de TMDb de cada obra.

\subsection{Motivación}

TMDb selecciona automáticamente el \textit{poster} con mayor número de votos para cada región lingüística. Sin embargo, en algunos casos el \textit{poster} automático no es el más representativo:
\begin{itemize}
\item El póster en español puede ser visualmente inferior al en inglés.
\item Para series antiguas, el póster preferido puede ser el original de su estreno y no el rediseñado recientemente.
\item Algunos títulos tienen pósters promocionales con mejor reconocimiento visual que el póster oficial.
\end{itemize}

Las sobrescrituras pueden apuntar tanto a imágenes alojadas en la carpeta pública del proyecto como a recursos externos o a rutas de la propia galería de TMDb, lo que ofrece flexibilidad suficiente para adaptar la identidad visual de cada ficha.

\subsection{Funcionamiento general}

Cuando la aplicación necesita mostrar una imagen principal, comprueba primero si existe una sobrescritura definida para ese título. Si la encuentra, utiliza esa referencia; en caso contrario, mantiene el comportamiento por defecto y recurre al material obtenido automáticamente desde TMDb. Este mecanismo introduce una capa de personalización no intrusiva, ya que permite corregir casos puntuales sin alterar el resto del flujo de obtención de datos.

El sistema también dispone de una API interna para consultar y actualizar estas preferencias de forma dinámica. De este modo, la gestión del \textit{artwork} puede realizarse sin modificar manualmente el código fuente, lo que simplifica el mantenimiento y facilita futuras tareas de administración del catálogo.

Los tipos de recurso admitidos incluyen:
\begin{itemize}
\item Ruta relativa al \textit{public folder} (\texttt{/custom/...}).
\item Ruta de imagen de TMDb, resuelta automáticamente contra su CDN.
\item URL externa completa (\texttt{https://...}).
\end{itemize}

% -----------------------------------------------------------------------
\section{Integración con comunidad Trakt}
% -----------------------------------------------------------------------

\subsection{Sistema de comentarios}

La página de detalle incluye una pestaña «Comunidad» que muestra los comentarios públicos de Trakt para el título actual. API Route implementada: \texttt{/api/trakt/community/comments}.

\textbf{Ordenación disponible:}
\begin{itemize}
\item \texttt{likes}: comentarios con más \textit{likes} primero (default).
\item \texttt{newest}: más recientesprimero.
\item \texttt{oldest}: históricos primero.
\item \texttt{replies}: con más respuestas primero.
\end{itemize}

\textbf{Paginación:} 20 comentarios por página, con carga bajo demanda al hacer \textit{scroll} hasta el final (\texttt{IntersectionObserver}).

\textbf{Renderizado:} cada comentario muestra:
\begin{itemize}
\item Avatar y username del autor (link a perfil Trakt).
\item Texto del comentario (Markdown renderizado con \texttt{react-markdown}).
\item \textit{Spoiler warning}: los comentarios marcados como \textit{spoiler} en Trakt están ocultos por defecto, requieren clic para expandir.
\item Contador de \textit{likes} y respuestas.
\item Fecha de publicación relativa («hace 3 días»).
\end{itemize}

\subsection{Listas de la comunidad}

La ruta \texttt{/api/trakt/community/lists} obtiene listas públicas populares de Trakt para un título:

\begin{verbatim}
GET /api/trakt/community/lists?type=movie&tmdbId=550
    &tab=popular&page=1&limit=10
\end{verbatim}

\textbf{Pestañas disponibles:}
\begin{itemize}
\item \texttt{popular}: listas con más \textit{likes}.
\item \texttt{trending}: listas con actividad reciente.
\item \texttt{personal}: listas del usuario autenticado que contienen este título.
\end{itemize}

Cada lista muestra: nombre, descripción, autor, número de ítems, número de \textit{likes}, y botón de acceso rápido para ver la lista completa en \texttt{/lists/trakt/[listId]}.

\subsection{Estadísticas de temporada}

Para series, la pestaña «Comunidad» incluye ratings promedio de Trakt por temporada y episodio, obtenidos de \texttt{/api/trakt/community/seasons}.

% -----------------------------------------------------------------------
\section{Integración con Letterboxd}
% -----------------------------------------------------------------------

Letterboxd es la plataforma de referencia para cinéfilos y críticos de cine, con más de 10 millones de usuarios. Aunque no tiene API pública, \textit{The Show Verse} implementa integración mediante scraping controlado.

\subsection{Búsqueda de películas en Letterboxd}

La API Route \texttt{/api/links/letterboxd} construye URLs de búsqueda en Letterboxd:

\begin{verbatim}
GET /api/links/letterboxd?title=Inception&year=2010

Response:
{
  "searchUrl": "https://letterboxd.com/search/Inception/",
  "filmSlug": "inception-2010",
  "directUrl": "https://letterboxd.com/film/inception-2010/"
}
\end{verbatim}

Algoritmo de construcción del slug:
\begin{enumerate}
\item Normalizar título: minúsculas, eliminar caracteres especiales, espacios $\rightarrow$ guiones.
\item Añadir año si el título es ambiguo (ej. «Dune» $\rightarrow$ \texttt{dune-2021} vs. \texttt{dune-1984}).
\item Validar existencia mediante \texttt{HEAD} a la URL construida (código 200 confirma).
\end{enumerate}

Si la URL directa falla, se devuelve la URL de búsqueda como fallback.

\subsection{Botón de acceso rápido}

La página de detalle incluye un botón «Ver en Letterboxd» en la sección de enlaces externos que abre la película directamente en Letterboxd en nueva pestaña. Útil para usuarios que desean:
\begin{itemize}
\item Leer reseñas detalladas de la comunidad cinéfila.
\item Añadir la película a sus listas personales de Letterboxd.
\item Ver las listas de la comunidad que incluyen esta película.
\end{itemize}

% -----------------------------------------------------------------------
\section{Manejo de errores y estados de carga}
% -----------------------------------------------------------------------

\subsection{Estrategia de \textit{graceful degradation}}

Todas las integraciones con APIs externas implementan un patrón de degradación elegante mediante gestión de excepciones que permite continuar con funcionalidad reducida en lugar de bloquear la interfaz completa. Cuando una API externa no responde o devuelve un error, la aplicación captura la excepción, registra el problema en consola para diagnóstico, y asigna valores nulos o por defecto a los datos que no pudieron obtenerse. 

Por ejemplo, si la API de OMDb no está disponible al cargar los detalles de una película, el rating de IMDb simplemente se muestra como «N/A» en lugar de impedir que se renderice toda la tarjeta. De esta forma, el usuario puede seguir accediendo al resto de información proveniente de TMDb y Trakt sin interrupciones. Este patrón se aplica consistentemente en todas las peticiones a servicios externos, garantizando que ningún fallo aislado comprometa la experiencia general.

\subsection{Skeleton loaders}

Durante la carga de datos, los componentes renderizan \textit{skeletons} animados que simulan la estructura visual del contenido final. Estos componentes de carga utilizan animaciones de pulso implementadas con Tailwind CSS que crean un efecto de brillo sutil sobre fondos grises, proporcionando feedback visual inmediato al usuario mientras se completan las peticiones asíncronas.

El sistema incluye diferentes tipos de \textit{skeletons} especializados según el contenido que representan. El \textit{PosterSkeleton} reproduce la proporción 2:3 de los pósters de películas y series. El \textit{CardSkeleton} simula tarjetas completas con bloques rectangulares que representan título, descripción y metadatos. El \textit{GridSkeleton} genera una cuadrícula de \textit{skeletons} para listas de resultados. El \textit{ScoreboardSkeleton} muestra una barra con círculos animados que anticipan la posición de los indicadores de puntuación.

Todos estos componentes se definen de forma centralizada en un módulo dedicado y se reutilizan a lo largo de la aplicación, garantizando consistencia visual en los estados de carga.

\subsection{Mensajes de error contextuales}

Los errores críticos que requieren atención del usuario, como fallos de autenticación o timeouts de red, se comunican mediante notificaciones no intrusivas que aparecen temporalmente en la esquina superior derecha de la pantalla siguiendo el patrón \textit{toast}. Estas notificaciones se generan dinámicamente como elementos del DOM con estilos diferenciados según el tipo de mensaje (error, advertencia, éxito o información).

El sistema crea el elemento visual, lo inserta en el documento, y programa su eliminación automática tras cinco segundos. Este comportamiento temporal evita que el usuario tenga que cerrar manualmente cada notificación y previene la acumulación de mensajes en pantalla, manteniendo la interfaz limpia mientras se proporciona feedback claro sobre operaciones críticas.

\subsection{Estados vacíos}

Las listas vacías se gestionan con componentes específicos que muestran ilustraciones y mensajes motivacionales en lugar de dejar el espacio en blanco. Cuando el usuario no tiene favoritos, historial de visionado o elementos en su lista de pendientes, se renderiza un estado vacío ilustrado que explica la situación e invita a la acción.

Estos estados incluyen un icono representativo de gran tamaño, un título que describe el estado vacío, un mensaje secundario que explica cómo poblarlo, y un botón de llamada a la acción que redirige al usuario hacia la funcionalidad relevante. Por ejemplo, el estado vacío de favoritos guía al usuario hacia la página de descubrimiento de contenido, facilitando que comience a explorar y agregar títulos a su colección personal. Este enfoque mejora la primera experiencia del usuario y reduce la fricción en la adopción de funcionalidades.

% -----------------------------------------------------------------------
\section{Diseño responsive y adaptación móvil}
% -----------------------------------------------------------------------

\subsection{Breakpoints y estrategia mobile-first}

El diseño utiliza los breakpoints estándar de Tailwind CSS:

\begin{table}[!htbp]
\begin{center}
\begin{tabular}{| l | l | l |}
\hline
\textbf{Breakpoint} & \textbf{Ancho mínimo} & \textbf{Dispositivos objetivo} \\
\hline
\hline
\texttt{sm} & 640\,px & Móviles grandes (landscape) \\
\hline
\texttt{md} & 768\,px & Tablets \\
\hline
\texttt{lg} & 1024\,px & Laptops \\
\hline
\texttt{xl} & 1280\,px & Desktops \\
\hline
\texttt{2xl} & 1536\,px & Pantallas grandes \\
\hline
\end{tabular}
\caption{Breakpoints responsive}
\label{tab:breakpoints}
\end{center}
\end{table}

La estrategia \textit{mobile-first} implica que todos los estilos base se definen para móvil (320\,px), y se sobrescriben progresivamente para pantallas más grandes:

\begin{verbatim}
<div className="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4">

\end{verbatim}

\subsection{Barra de navegación adaptativa}

El componente Navbar implementa dos arquitecturas completamente diferentes según el dispositivo:

\textbf{Desktop (lg+):}
\begin{itemize}
\item Barra horizontal superior con tres zonas (logo, búsqueda, links).
\item Búsqueda siempre visible en centro.
\item Todos los links principales visibles simultáneamente.
\item Dropdown de avatar en esquina superior derecha.
\end{itemize}

\textbf{Móvil (<lg):}
\begin{itemize}
\item Barra superior simplificada: logo centrado + botón menú + botón búsqueda.
\item \textit{Tab bar} inferior fijo con 4 secciones principales (Inicio, Descubrir, Favoritos, Perfil).
\item Drawer lateral deslizante (\texttt{translate-x}) para menú completo.
\item Búsqueda en \textit{overlay} a pantalla completa.
\end{itemize}

La transición entre modos ocurre automáticamente en \texttt{1024px} sin recarga de página.

\subsection{Imágenes responsive}

El componente \texttt{<Image>} de Next.js genera automáticamente \texttt{srcset} con múltiples resoluciones:

\begin{verbatim}
<Image
  src="/poster.jpg"
  width={300}
  height={450}
  sizes="(max-width: 768px) 50vw, 
         (max-width: 1024px) 33vw, 
         25vw"
  alt="Poster"
/>
\end{verbatim}

El navegador selecciona la resolución óptima según el tamaño de renderizado real. Resultado: ahorro de ancho de banda en móviles (imagen 300\,px en lugar de 1000\,px).

% -----------------------------------------------------------------------
\section{Plan de calidad: estrategia de testing}
% -----------------------------------------------------------------------

El proyecto no incluyó una suite de tests automatizados durante su desarrollo. La validación se realizó mediante pruebas manuales sistemáticas en navegador, una decisión pragmática dada la velocidad de iteración durante el prototipado. Esta limitación técnica debe abordarse antes de escalar el proyecto a producción.

La estrategia propuesta para futuras iteraciones incluye \textbf{Vitest} para pruebas unitarias e integración, \textbf{React Testing Library} para validar componentes desde el comportamiento del usuario, \textbf{MSW} para simular respuestas de APIs externas, y \textbf{Playwright} para pruebas \textit{end-to-end} de flujos completos.

Los módulos que requieren mayor cobertura de tests son: algoritmo de caché y deduplicación en vuelo, middleware de detección de bots, flujo OAuth y gestión de tokens, transformaciones de datos entre Trakt y TMDb, y algoritmos de selección de imágenes. Los componentes de presentación pura tienen menor necesidad de cobertura al aportar menos valor en la validación automatizada.